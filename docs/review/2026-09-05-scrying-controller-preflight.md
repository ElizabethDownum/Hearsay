# Recovered scrying correction — controller preflight

This is root's measured planning check, not independent approval or implemented
magic verification. The frozen recovered draft remains SHA-256
EBACFF982443BA604006D33CE62F5AD9AA11D95FE08E4C3DA70DB523A91176ED.

The exact authored `sameRefs` and conditional nested-copy snippets were extracted
from that draft and applied only in memory to the real current `enemyDigest`.
The probe uses its real state and rules. It stages subjectful leads and worked
nights explicitly: Task 3's own residue feature has null subject, so this does
not claim an actual scrying-to-runaround gameplay route.

| Variant | Exact physical clone | Different residue | Physical vs legacy | Exact legacy clone | Nested identity shared |
| --- | ---: | ---: | ---: | ---: | --- |
| Baseline | 1 | 1 | 1 | 1 | yes |
| Comparator correction | 1 | 2 | 2 | 1 | yes |
| Both authored corrections | 1 | 2 | 2 | 1 | no |

Counts are emitted runaround features. The final variant also asserts mutation
isolation, pure input state, preserved absent legacy keys and matching tail-drop
counts. Native probe and scoped lint exit0. Initial sandbox loader failure is
preserved separately and is not assertion evidence. Artifacts:
../../.superpowers/sdd/task-3-validation/review-corrections.mjs and
review-corrections-native.{stdout.log,stderr.log,exit.txt}.

The separate count/syntax check finds79 proposed cases and no complete-block
syntax diagnostics. It has not executed those79 gameplay tests. Required-marker
and colliding-question controls are still part of pending focused plan re-review.

Task4 insertion-order seam: keep both feature-kind required-marker preconditions
before either physical-ref value branch can continue. The saved Task4 draft does
contain a night-visit marker precondition; the remaining question is precise
placement relative to Task3's existing arcane-residue precondition. This is not
a claim that the Task4 guard is absent, nor a mandate for general corruption
hardening of every future derived feature.

Task2 is now implemented at dc114da and locally gated, with independent approval
pending. Before Task3 executes, reconcile the actual predecessor source and keep
Task2's document fields through every widened union/projection. Its planned
shared-auditor extraction must also migrate the new forensics caller and remove
the temporary exact-copy helper without weakening either existing pillar.
