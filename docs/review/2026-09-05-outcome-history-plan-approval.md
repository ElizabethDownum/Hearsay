# Independent Task5A2 private outcome-history proposal review — 2026-09-05

**Verdict: Approved-for-base-reconciliation. 0 Critical / 0 Important / 0 Minor.**
The exact proposed recording changes conform to the narrow outcome-history laws.
No new conditional code amendment is required by this review. Approval applies to
the frozen proposal and its explicitly shared virtual R16/R17 predecessors; it
does not approve production implementation, the watch mechanisms themselves, or
an unmeasured future magic/prose baseline.

Reviewer: independent native Codex, inheriting root capability and matching the
root Codex GPT-6 author. The Task5A association review was not rerun or modified.
No production/test/app/config/docs/index changes, commits, external calls,
children, installs or shared-AI writes occurred. Task3C1 remained the sole
production writer. PowerShell host version was verified as7.6.5; native output was
captured as binary before UTF8 display and real subprocess exits were saved.

## Exact inputs and isolated baseline

Read the complete brief, frozen draft/report/constraints, exact patch and new test
body, preparer, compiler/lint host, Vitest host, compatibility host, manifests and
relative source maps. Authority includes the design-spec debrief/every-operation
requirement, R8/R9, carried Plan11 speech/phase/serialization/epistemic constraints,
the watch execution amendment, and Task3/4/5A1 interface seams.

- Frozen draft SHA256:
  `54e43e894b1502129894cff4fb91c2b933291e3d5a6d9d6cf823fcc0a850af5b`.
- Embedded four-file patch equals the original saved patch and independently
  regenerated patch. SHA256:
  `dfa8124249c7b4fdc76f6803451ee62b51000482bf3dbca9d0c1740e44de311c`.
- The complete new test body embedded in the draft equals the frozen
  `task-5a2-outcome-tests.txt` and both original/regenerated virtual bodies.
- All six virtual map bodies equal the original map: three Task5A2 production
  proposals, one modified existing suite, one new suite, and the separately
  identified common R16 evaluator. The execution proposal is built on the exact
  R17 execution source. No existing assertion line is removed.
- Committed validation source is
  `09e54582f0a5fc9505e396bd1910c800c7413fff`, copied from `git archive`.
  Its227 required source/test/config/runtime/asset-manifest files remained
  byte-identical throughout. No live Task3C1 source supplied partial inputs.
- Common R16 evaluator text SHA256:
  `27bab2d38e04ce6f5b88f5ed7afb99618daf12d7db7c3496f7960006da624269`.
  Common R17 execution text SHA256:
  `af5b384f3e65d3c6007cf228974be990a7add2630a4c1d4ffabb29123b5bbf11`.
  Both comparison sides use those same predecessors; only the declared Task5A2
  delta distinguishes them. R20 changes digest/forensics, outside these proposal
  files and the affected suites, so the measured affected count remains239.
- Before/after hashes verify all129 historical Task5A2/R16/R17 artifacts protected
  by this review unchanged. No old output writer was run in place.

All evidence is under `.superpowers/sdd/task-5a2-native-plan-review/`. The isolated
snapshot began, and remains, under `node_modules/hearsay-baseline` inside that
directory. Copied original proof hosts run from that isolated root, so their
historical relative output paths resolve only inside the new reviewer-owned tree.
The preparer's sole host edit changes its expected cwd basename from `hearsay`
to `hearsay-baseline`; proposal transformation code and assertions are unchanged.
The enforcement-only config changes only the selected suite to the exact virtual
view suite. No root discovery exclusions were changed.

`input-integrity.json`, `baseline-manifest.json`, protected before/after manifests,
`final-integrity.json` and per-mode invocation JSONs preserve exact provenance.
Native root `vitest list --filesOnly --json` exited0 and listed119 live test files
with zero review-snapshot paths (`discovery-proof.json`). That is an exclusion
check, not a passing119-file production suite.

## Charge dispositions and code tracing

1. **PASS — capture every actual result at the existing result seams.**
   `recordDirectiveOutcome` clones the raw `DirectiveExecutionResult` before the
   report builder can alter disclosure, candor or claim traits. The queue validates
   the already-received route before capture, then retains a result before either
   the no-route or empty-projection return. Direct-player refusal separately records
   exactly one result and associates its actual immediate response packet. Direct
   acceptance schedules work and acknowledges receipt without an invented execution
   result. The existing abort helper's explicit no-queue branch also records without
   allocating a packet; no current production caller passes that optional false
   argument, so this branch is a preserved helper contract, not a claimed live path.

   Inspection covered all result callers, not just the fifteen authored cases:
   `execution.ts` abort/receipt paths; the shared application completion helper;
   courier delivery; ordinary observe/tell; inquiry answer; and worked-night latch;
   plus `network/recruitment.ts:488` for an actually heard candidate response.
   Posting removal/occupation, rendezvous attendance/miss, inquiry start,
   interrogation asking, successful/stale cancellation, expiry, local refusal and
   absent-opportunity exhaustion all reach the existing queue/abort seams. Their
   existing state guards prevent terminal results from being manufactured each
   tick. Recruitment's separate heard hesitation/final response can legitimately
   produce separate occurrences. In-progress setup/acceptance alone is not falsely
   recorded as performed work. No reporting call site was removed or bypassed.

   The normal watch transaction runs through real attempted work and inclusive
   expiry on common R16/R17. It retains each of eight actual nights and one existing
   expiry refusal even when no packet or HQ ledger row is produced. The historical
   pre-R17 premature/duplicate latch is not hidden in the recording comparison.

2. **PASS — detached occurrences and honest packet associations.**
   Each call appends a fresh occurrence and deep-clones its nested result. Repeated
   identical results are not deduplicated by content. The actual return value of
   `allocateNetworkMessage` is assigned directly to that occurrence, including the
   direct-response path; null remains no queued packet. Original tests mutate raw
   claim/evidence/fact/action data after capture and mutate the retained claim
   afterward without changing the queued projection. Heard answers retain the raw
   heard claim and uncertainty; no claim is relabeled as factual truth.

   An independent pass over all twelve saved complete-world pairs resolved every
   non-null association to exactly one actual directive-report packet for the same
   directive:54 links, no repeated packet ID within one record's occurrences.
   The remaining27 occurrences have null packet IDs under the actual no-report
   policy. The81 rows comprise72 worked-night results and9 existing expiry refusals.

3. **PASS — enforcement first and actual boundary firing.**
   The very first runtime suite was the exact modified view suite:151/151 passed.
   Both new named prongs executed their existing seven-form firing tables, covering
   dotted, bracketed, optional, destructured, aliased-destructured, shorthand and
   private-field access. Both production absence checks and the extended concrete
   hidden-state twin passed. No scanner implementation or closed R15 mandate was
   widened. Existing non-vacuity and lawful-neighbor controls also ran.

   Original runtime cases verify spoken payload and received-report shapes stay
   free of private outcomes. An additional reviewer-owned twin changed retained
   tick, packet association, raw outcome/reason and claim count after queuing the
   report. Actual speech and observer feeds remained identical. Both player network
   intel and enemy network evidence actually fired; silence controls did not. Their
   complete resulting stores stayed identical across metadata twins, with no
   private sentinel or outcome fields. This extra check passed with all fifteen
   original cases:16/16, separately from the239-case proposal gate.

4. **PASS — atomic existing rejection/pure builder and unchanged old state.**
   Self-hop and duplicate-route checks precede the new history mutation; the native
   rejected-self-hop case preserves the complete world hash. The builder alone
   remains pure and leaves optional outcomes absent. No new ID allocation, message,
   timing/resource/RNG/decision/schedule change was introduced. `outcomes` is lazy
   and no-result worlds retain their bytes.

   All twelve full-world pairs compare equal after removing **only**
   `DirectiveRecord.outcomes`: the full world and every recorded execution
   transition remain equal. The runtime comparison was independently repeated;
   a separate Python audit repeated full-value equality with exactly that single
   stripping operation and checked the54 packet associations. It did not remove
   receipt data, claims, evidence, intel, message counters, decisions or physics.

5. **PASS — exact inventory and native gates.**
   The new file has9 ordinary cases plus tables of4 and2 =15. Two additional named
   prongs each generate an absence check and firing check =4 more. Total19; no
   old test identities or assertions are retired. Licensed implementation remains
   three production files, one modified suite and one new suite. The sixth virtual
   body is the common R16 validation predecessor, not a fourth Task5A2 production
   proposal. Native affected total239/7 is8 reports +11 execution +15 enemy-orders
   +151 view +15 outcome-history +27 spoken-content-law +12 recruitment-lifecycle.
   Both real compiler configurations and all six virtual lint bodies pass with
   zero diagnostics, errors and warnings. Actual future implementation count must
   be measured after all legitimate predecessors;239 is this affected probe only.

6. **PASS at plan level; actual-base reconciliation remains required.**
   The additive `DirectiveOutcomeRecord` and optional `DirectiveRecord.outcomes`
   fields do not change `ReportedFieldObservation`, physical receipt/ref variants,
   the raw observation projector or field-report transport. Task3's residue report
   atom/receipt and retry closure rules, Task4's marked presence/night-visit fields,
   and Task5A1's private `NetworkSpeech.reportRoots` must remain in their shared
   `directives/types.ts` file. In particular, retain the Task5A1 field that precedes
   the end of `NetworkSpeech`; install only the additive hunks, never the old full
   types snapshot. `execution.ts` must retain actual committed R17 while accepting
   this recording delta. Task3's draft explicitly finds no new residue branch
   needed in `directives/evaluator.ts` or execution's local presence feed
   (Task3 draft1722); neither is a new sensory channel here.

   Task5A2 does not edit `threadOf`, so the adopted T5A-A1 séance membership correction
   remains intact. No future contract conflict like the earlier two-kind guard
   defect was found in these hunks. Approval does not install or certify future
   Task3/4/7A/5A1 source. Required order remains separately gated/reviewed R16, then
   R17, and actual magic/prose/5A1 reconciliation before Task5A2 implementation.

## Independently executed native results

Every listed process has complete binary-captured `.stdout.log`, `.stderr.log`,
`.exit.txt` and `.invocation.json` files in the reviewer directory. Exact native
escalation was confined to the already-known esbuild ancestor-read boundary;
no permissions or compiler/lint configurations were changed.

Enforcement FIRST, `enforcement-native`, exit0:

```text
 Test Files  1 passed (1)
      Tests  151 passed (151)
   Start at  22:36:22
   Duration  1.38s (transform 161ms, setup 0ms, collect 570ms, tests 351ms, environment 0ms, prepare 112ms)
```

Original mechanism RED, `base-native`, exit1:

```text
 Test Files  1 failed (1)
      Tests  12 failed | 3 passed (15)
   Start at  22:37:42
   Duration  1.06s (transform 251ms, setup 0ms, collect 601ms, tests 106ms, environment 0ms, prepare 80ms)
```

Failures are missing outcome/association assertions, not loader failures. The three
controls are pure builder, rejected-route atomicity and no false acceptance result.

Exact proposal plus affected suites, `regression-native`, exit0:

```text
 Test Files  7 passed (7)
      Tests  239 passed (239)
   Start at  22:37:47
   Duration  1.59s (transform 771ms, setup 0ms, collect 3.59s, tests 1.71s, environment 1ms, prepare 750ms)
```

Both compiler configurations and virtual lint, `preflight-native`, exit0:

```json
{
  "scope": "Task5A2 outcome-history proposal on virtual R16/R17 only; no implementation",
  "newSourceFiles": 3,
  "newTests": 19,
  "newFileTests": 15,
  "tsconfig.json": 0,
  "tsconfig.app.json": 0,
  "lintErrors": 0
}
```

Twelve complete pairs, `compatibility-native`, exit0:

```json
{
  "scope": "Task5A2 only; same virtual R16/R17 predecessors on both sides",
  "pairs": 12,
  "recordedOutcomes": 81,
  "allPreExistingFieldsEqual": true,
  "sha256": "0cfd3d1bdc832499baec9861f34b5cf6e318aec2b5af76707cd67dc0e6242832"
}
```

The digest equals the historical author result. Earlier90-outcome evidence is not
the common-R17 result and was neither overwritten nor presented as current.

Additional actual-boundary control with unchanged fifteen cases,
`boundary-v2-native`, exit0:

```text
 Test Files  1 passed (1)
      Tests  16 passed (16)
   Start at  22:43:06
   Duration  1.20s (transform 262ms, setup 0ms, collect 631ms, tests 182ms, environment 0ms, prepare 84ms)
```

## Reviewer vehicle correction and limitations

The first extra reviewer boundary vehicle failed one firing assertion: it expected
ordinary directive-report speech to append `intel.log`. Source inspection of pinned
`fieldwork.ts:34–60` establishes that `appendOwnNetwork` records this speech in
`intel.network`; `intel.log` receives other specifically projected observation kinds.
The fixture had emitted correct speech and reached the feed. This was a reviewer
destination-store assumption, not a failed author acceptance or gameplay defect.

The initial16-case result (15pass/1fail), original reviewer control and source map
remain saved as `boundary-native` and `boundary-sources.json`. A separately versioned
vehicle changes only that firing assertion to require exactly one network-intel row
matching the actual packet ID and spoken content. All author assertions and other
twin/silence assertions are unchanged. The versioned16-case result above passes.
`boundary-vehicle-disposition.json` records the exact old/new assertion and citation.

No full production `npm test`, build, soak or MC was run or claimed by this bounded
proposal review. Those implementation gates, complete simulation report comparison
and independent implemented-code review still apply on the actual future base.
The12-world proof is scoped metadata compatibility, not an execution or review of
all R16/R17/magic behavior. Deferred R18 cancellation eligibility, expiry phrasing
and interpreted-versus-received venue issues remain separate; recording their
existing results does not silently resolve or retune them.

No broad scanner hardening, route-validation redesign or false global atomicity
claim is implied. The existing validated route rejections and actual production
callers were checked within this proposal's recording scope. Final integrity
rehashing confirmed129 protected historical files,227 committed baseline files and
all six original virtual bodies unchanged.
