# Task 4 author report — complete draft, 2026-09-05

Native Codex author, inherited GPT-6 capability. Owned only the two ignored draft/report files. No Claude, children, production/test writes, runtime test execution, index operations, commits, network/install, UI/process control, or shared-memory writes.

Draft: C:/Users/eliza/Desktop/ClaudeFiles/hearsay/.superpowers/sdd/task-4-author-draft.md.
Readiness: **complete code and test bodies; both author self-reviews complete; ready for independent plan review and controller adoption. Not certified for immediate implementation dispatch.** Actual Task2/Task3 code, reviewed shared corrections, and the refreshed baseline remain controller preconditions.

## Complete authored scope

4A retains one separate historical witness using isolated gen:departed RNG after scenario retargeting. It selects unused content identity with deterministic fallback, real seven-field secret testimony and an outgoing subject relationship. Generated null fails departed-sane/rerolls; omission preserves hand-built compatibility; present undefined is rejected. No living NPC, edge, witness, belief or old draw is removed. Attachment binds the existing minted claim and copies both nested records.

4B supplies the complete 20-coin, once-per-campaign local action: finite current 15-aligned tick, frozen offered chapel/cathedral, [00:00,04:00), validations before debit/latch/intel/chronicle. Explicit magic provenance and null endpoints yield claimful testimony plus one edge-read without an invented NPC carrier, route, receive/emit pair, informant count or document marker. Campaign, economy, localParticipants/LOCAL_KINDS, exhaustive verb vocabulary and the real session save/replay path are specified exactly. No new composer UI.

4C supplies actual silent-tick avatar sighting -> held report -> real spoken receipt -> physical enemy evidence -> pure digest feature -> +1 generic exposure. Direct spymaster sighting has a real sighting record and no fabricated speech/receipt. Existing evidence is the only digest substrate; no nightVisits enemy ledger. One feature per(kind,subject), no carrier-profile identification. Player overhearing the marked presence report receives scene-presence, never a false watch.

4D supplies exact focused/full gates, actual-predecessor baseline capture, complete soak/MC comparator with four firing controls, count/size/timing evidence, staged commit discipline and independent accumulated review. Units are 4A,4B,4C1,4C2,4D with frontier implementer/reviewer floors; exact licensed files and commit subjects are in the draft.

## Source-informed premise repairs

The original symmetric-risk assertion was false: noticedByObserver rejects presence; ingestEnemyItem lacked presence evidence; recordAndIngest skipped silent enemy capture; player presence is watch-occupation filtered. A retained report alone had no sketch/exposure effect.

Root accepted the narrow avatar chapel-night rule independently of hidden spell state. The draft preserves that amendment explicitly, including its intentional ordinary-no-magic night-visit delta. This matches the existing avatar caught-in-act scenario scope; general NPC night-attendance anomaly gossip remains deferred. No-encounter worlds retain bytes apart from generated metadata.

Further source checks found:
- runEnemyDay returns for an empty observer roster. Preserve that enemy-off law. The direct consequence test retains the actual guard roster with the guard physically elsewhere; a diagnostic observer-empty fixture can hold direct evidence without nightly digestion.
- threadOf includes every claimId-bearing chronicle event, including the new séance. campaignMetrics previously treated every non-inject entry as a telling. Exact small 4A narrowing now reads heardBy only for kind telling; the real ritual test proves all metrics unchanged while the séance remains in its family thread.
- Physical refs invalidate the old flat sameRefs/copy assumptions. Final Task4 code compares exact nightVisit identity and detaches its nested record. It preserves the separately reviewed residue counterparts.
- A night-visit feature must require its physical marker before auditor branch selection. The negative control strips it and supplies an unrelated asking with matching null IDs; that must fail.

## Shared predecessor identity and corrections

Read frozen Task3 AUTHOR draft SHA256:
16655291E98D8CEF7A7FB3E8EF84DE28CBAC41B7C52C2D066D36DA9259A1F00B.
This records the artifact actually inspected, not an implemented/future hash. Root is separately reconciling its review corrections; the eventual revised artifact/interface must be reverified before adoption.

Contract used: IntelEntry.provenance optional MagicProvenance, string via retained; sourceOf/sourceKey/sourceLabel/isMagic/singleInformantChannel; scene-presence; null endpoint route/Codex guards; PhysicalReceipt {tick,observer,messageId} stays on EvidenceEntry only; exact physical data on SketchEvidenceRef; generic ingestEnemyItem physicalReceipt; extracted permanent auditSketch helper; speech OR residue capture guard; positive Extract utterance|network voicings narrowing. Task4 leaves world.magic and magic.ts scry-only.

Three narrow shared residue checks were sent to root without editing the frozen Task3 artifact: sameRefs must compare residue identity; runaround must detach nested residue copies; residue feature kind must require its physical marker before a legacy null-ID collision can be considered. Root explicitly accepted the latter under R5/R7 and assigned its correction/control to Task3 author. Task4 contains its complete corresponding night-visit code and controls.

Task2 marker projections remain preserved everywhere reports are touched; séance emits no marker. Missing predecessor interfaces require a controller amendment, not an improvised alternate mechanism.

## Author verification and limits

Source read began at caa5a38, reverified at 0241fc8b82a633856a47c625d2b807753e9fa138, and final static source check used the current a53551d6e47fc575f7c306f43f9fa0ff836250cc tree. Other workers' changes were preserved. Historical 1713/113 is not the future dispatch floor.

Forward self-review traced generation, attachment, action, session, actual perception, physical report receipt, evidence, pure digest and consequence. Reverse self-review checked gates, audit corruption controls, ref identity/copies, replay, fixture reachability and retained truth back to the constraints. Corrected source/API mistakes: world.coin, runUntil(world,endTick,rules), sourceKey(SourceRow), the real secrets/dossier test paths, and the metrics/thread consumer.

Read-only Node/TypeScript CompilerHost overlay extracted 11 complete authored modules (production/helpers/tests and the permanent auditor), applied the Task4 integration splices and additive Task3 interface scaffolding in memory, and inserted Task3's stated positive voicings narrowing. Final checks across both root and app project file sets reported zero diagnostics. Comparator JavaScript parsing also reported zero diagnostics. This is static source/API evidence using an explicitly constructed pending interface; it does not prove the unimplemented predecessor runtime, the actual final project typecheck, or any proposed behavior. No authored tests, lint, builds, soak, MC or runtime code were executed by this author.

All proposed test bodies are saved, including non-vacuous direct/delayed reporting, omission, ordinary no-magic twin, hidden-state getter guard, current/offered tick walls, alias/serialization boundaries, real session replay, nightly exposure and physical-auditor corruption controls. Runtime success, exact report equality and the real suite floor must be established during authorized implementation.

No unresolved user decision remains. Controller may now independently review/adopt this complete draft, then verify its real predecessor before dispatch.
