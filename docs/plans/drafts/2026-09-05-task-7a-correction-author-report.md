# Task 7A correction author report — COMPLETE PROPOSAL, review pending

Author: inherited highest controller/author capability in the former independent review thread, now explicitly switched to the author seat. The correction requires a new independent reviewer; no self-approval is claimed.

Original frozen draft/report/review evidence are preserved. Exact scope is renderer corrections plus additive tests in the two already-proposed test files; original UI/name-selector code and all44 prior authored cases stay intact. The completed amendment is `.superpowers/sdd/task-7a-corrected-author-draft.md`; its constraints are `task-7a-correction-constraints.md`. The final manifest records SHA256 identities for these documents, exact virtual inputs, validation hosts, logs and proof files.

Local presentation strategy: preserve `NameOf(id): string` and the existing avatar label `you`; select grammatical agreement solely from that explicitly returned pronoun, including subject, neutral object qualifier and source. Neutral object treatment also removes unsupported victim/owner/opponent/betrothal/nursing roles from fixed/unspecified-role predicates. No engine facts or identifiers change.

## Executed native author evidence

| Check | Actual result |
| --- | --- |
| Original renderer plus all corrected permanent tests (RED) | Native exit1;78 cases,27 targeted assertion failures/51 passes. All44 original cases pass; seven already-neutral predicate controls pass. |
| Corrected renderer with identical test inputs (GREEN) | Native exit0;78/78 cases in one virtual probe entry. |
| Both real compiler configurations over the proposal | Native preflight exit0; zero diagnostics in each. |
| Virtual future registry key | One actual TS2741 diagnostic requiring `future-prose-sentinel` in the renderer; current-key control is clean. |
| Predicate metadata equality | All24 registry values equal current production; the public Record facade and literal-key completeness mechanism remain unchanged. |
| Virtual lint | Zero errors across all11 proposed files under actual ESLint configuration. |
| Mechanical self-checks | Native exit0; exact draft bodies equal executed inputs; only renderer differs RED->GREEN; only three proposed files differ original->corrected; original44 cases retained;17 neutral fixed+7 explicit relational keys partition the actual24; all15 original preflight assertions remain text-identical. |

RED failures are ten revised predicate-role branches, three real-objectifier cases, nine present-tense subject cases, three fallback/object/source grammar cases, and two actual enrolled-avatar cases. These are assertions, not loader failures. Full raw stdout/stderr, child exit files, command arrays and before/after historical hashes are preserved in `task-7a-correction-validation/red.*`, `green.*`, and `preflight.*`. No assertion, mechanism, threshold, formula or seed was weakened.

Commands actually executed, from the Hearsay repository:

```text
node node_modules/vitest/vitest.mjs run --config .superpowers/sdd/task-7a-correction-validation/vitest-red.config.mjs
node node_modules/vitest/vitest.mjs run --config .superpowers/sdd/task-7a-correction-validation/vitest-green.config.mjs
node .superpowers/sdd/task-7a-correction-validation/preflight.mjs
node .superpowers/sdd/task-7a-correction-validation/check.mjs
```

The first three were invoked by `run.py`, which captures native stdout/stderr as bytes and records the child exit before any output extraction. Exact sandbox escalations for native Vitest/esbuild were accepted; no permission/config workaround was used. The self-check executed normally. These are author-run virtual proposal checks, not independently reviewed code or additions to the production suite.

## Code and case inventory

Only three of the original11 proposed file bodies change:

- `src/content/render.ts`: ten fixed/unspecified-role object branches use the neutral qualifier while retaining the complete proposition. The existing public `you` label selects second-person agreement for nine present-tense templates, the unknown fallback, neutral object qualifiers and attribution (capitalized `You swear it.`). API remains `NameOf(id): string`, `renderClaim(ClaimText, NameOf): string`.
- `tests/content/render.test.ts`: all original33 tests retained as an exact source prefix, then32 additions:17 fixed-proposition tests each exercising null/named/SOMEONE,3 actual-objectifier regressions,9 present-tense subject cases,1 unknown fallback,1 neutral object and1 attribution case. Total65.
- `tests/sim/claim-names.test.ts`: all original4 cases retained as an exact source prefix, then2 real-enrolled-avatar selector->renderer regressions for subject and attribution. Total6.

The other8 proposed files are byte-identical to the original virtual proposal: predicates, fieldwork selector, ClaimReading, EvidenceBoard, Directives, DayPlanner, main, and the7-case UI test file. No tests retired/migrated. Total65+6+7=78 = old44+34. Implementation chunk arithmetic is actual predecessor B/F -> B+71/F+2 for7A1 -> B+78/F+3 for7A2. The future Task4 floor remains unknown; no future count/hash was invented.

The amendment gives exact code and a per-predicate disposition table. The additional I1 branches were explicitly disclosed to root: theft does not make the added person a victim, lineage does not assign ownership, throne plans do not name a current monarch, card/regatta claims do not assign opponents, betrothal does not assign a fiance, and nursing does not assign a patient. Seven explicit relational/transitive predicate arguments remain intact. This is within the accepted fixed-proposition/unspecified-role correction, not new game behavior.

## Validation basis and unchanged historical artifacts

Authoring base is `3bc637ff82d6a5588e6d91462715f3e4a640ba95`, with relevant source equal to dc114da. During the runs a separate production worker owned digest.ts and forensics.test.ts. Root explicitly authorized holding their `git show 3bc637f:<path>` texts as validation-only baseline inputs. The host reads those two snapshots separately; they are outside the11 proposed-file map and never become proposal patches or production writes. All other relevant prose/UI inputs stay frozen.

After these checks completed, root reported the separate R20 commit `09e54582f0a5fc9505e396bd1910c800c7413fff`. These results are intentionally attributed to the already-run isolated3bc637f basis; no exact09e5458 gate is claimed and no redundant rerun was substituted for the recorded evidence.

The versioned preflight/config diffs disclose only changed input/output paths, the two authorized baseline read overrides, and updated reported counts. Self-check proves all15 original preflight assertion calls are unchanged. RED/GREEN maps differ only at the renderer. Both baseline-file hashes, all11 proposal hashes, and exact amended-code-fence equality are machine checked. `red/green/preflight.manifest.json` each prove historical author files, old validation outputs and completed independent review evidence unchanged before/after.

## Two adversarial author passes

**Pass1 — semantics, API and arithmetic:** checked all24 keys against the real registry and glossary.17 fixed/unspecified-role clauses use neutral naming;7 explicit relational/transitive clauses retain their argument semantics. Traced null/SOMEONE through the unchanged guarded name helper; source names are looked up once and `SOMEONE` never reaches the callback. Checked zero/count/place emphasis and no credence reads. The pure string helper introduces no identity/state input. Preserved original tests exactly; measured78 matches65+6+7, with no production suite increase claimed. Both compiler configurations and actual lint validate source APIs. No threshold/magnitude/resource-reset or timing issue is introduced.

**Pass2 — reverse order, integration and evidence:** started with actual-avatar attribution/subject cases, then neutral-object agreement, unknown fallback, objectifier deltas and fixed-clause tests. The new tests fire against the old renderer while all old44 controls stay green. Followed the unchanged selector and all unchanged panel/main inputs to confirm no name, control-mode, offered-state, action, payload or hidden-data mutation entered the correction. Reconciled the separate R20 read snapshots against the11 proposed-file inventory, then proved exact draft->preflight->Vitest source equality and unchanged15 preflight assertions. All loops are bounded arrays/registry folds; there is no wait on a resetting resource. Whole-file installation remains explicitly prohibited, and actual magic predecessor reconciliation stays open.

No unresolved author self-review finding remains. This is not an independent verdict; a new thread must review this corrected proposal.

## Vehicle corrections and disclosures

- Initial preparation stopped on its own `once()` assertion because the original preflight names the draft twice (input and output metadata). The generator was corrected to target the exact input statement, then the remaining metadata occurrence separately. No original file or assertion changed. This was preparation integrity checking, not a gameplay RED.
- The original authored tests remain exact prefixes, so extra imports are valid top-level module declarations after the inherited block. Both TypeScript and lint accept the complete permanent file bodies; no import/type shim or configuration exemption was added.
- The explicit `you` strategy is formatting-only and preserves the current labels/API. A future localization or richer grammatical-person contract must extend it deliberately; this task does not introduce that system.
- No production/test/app/config/docs/index edits, commits, pushes, child agents, packages, UI automation or external calls were made. Only the four licensed corrected artifacts/output area were written. The canonical author skill/template and shared context were read-only.

## Handoff boundary

The exact corrected draft and report are complete author proposals. Freeze their manifest identities and obtain a fresh independent plan review; this former reviewer is now the correction author and cannot approve its own work. Actual approved Task4-base reconciliation, landed implementation, full production gates, bundle-size reporting and independent code review remain required. Task7B/debrief vocabulary/slots and richer prose APIs remain deferred to their existing owners.
