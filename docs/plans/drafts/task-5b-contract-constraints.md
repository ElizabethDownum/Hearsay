# Task 5B composition contract correction constraints

13 September 2026. Author-only correction under root's explicit brief. Timing clearance
was received before tests, compilers or heavy subprocesses. No child agents.

- Own only this host's three named markdown artifacts and task-5b-contract-validation.
  Production paths are read-only even in the detached host. Root owns primary docs,
  index and commits; other UI authors/reviewers are active. No external/shared AI writes.
- Actual base ef7fa3848ac3e1e71e2490576f1af9ea4f77eb64; 356 archived files unchanged.
  Preserve all 592 original author and 457 reviewer inventory entries and 24 copied
  fixed inputs. No frozen cache is an input or a test host.
- Exactly threads.ts, index.ts and additive composition.test.ts differ from the frozen
  278 proposal. All 29 upstream bodies, calendar.ts and overlay.ts are unchanged.
  Five installation chunks remain; only chunk 5B5's three bodies change.
- Separate retained future observation/acquisition/digest rows from current operation
  evidence/features in the additive beyondClock section. Preserve unknown-time rows
  as null/unrecorded; keep exact indexes, every-operation/orphan reachability and purity.
- Ending resolution pointers must resolve uniquely against retained status-specific
  records. Preserve raw missing/inconsistent metadata, and label ambiguous/unresolvable
  identities inconsistent. Do not rerun the referee or invent missing history.
- No gameplay thresholds, current council/informant/guard rosters, RNG or clocks may
  be used to recalculate historical outcome truth. Consistency checks source identities;
  it does not certify reconstructed historical credence or missing belief-edit history.
- Preserve every original 278 assertion and case identity. Add all five reviewer
  regressions plus 30 adjacent controls: final 313, no migrations. RED/GREEN test bytes
  are identical. Keep the initial fixture compiler failure and second-pass RED intact.
- No schema/recording/session/gameplay/UI changes or new value exports. Both actual
  compilers, 34-body lint and four mandatory law firings at both changed paths pass.
- Capture immutable binary stdout/stderr/exit, argv/cwd/executable and executed source
  maps before formatting. Copy compiler/lint diagnostics into each run prefix. Never
  overwrite old native evidence, original maps or caches; exclude only disposable
  owned caches and the inventory itself from the final inventory.
- No package installation, ACL/global/project configuration change, network, full-game,
  build, soak, Monte Carlo, production installation, staging, commit or push is authorized
  by this proposal. Independent correction review and actual-base reconciliation precede
  model/UI certification and implementation; Plan 10 remains outside scope.
