# Task 4 author report — PARTIAL, 2026-09-05

Stopped on root's instruction after Ellie invoked handoff to restart the computer.
Native Codex author, inherited GPT-6 capability. No Claude, children, network/install,
production/test changes, test execution, index operations, commits, or shared-memory writes.

Draft: `C:/Users/eliza/Desktop/ClaudeFiles/hearsay/.superpowers/sdd/task-4-author-draft.md`.
Readiness: **NOT DISPATCH-READY**. Source inspected at caa5a38; pending Task 2/3 changes
must be reverified. Current controller baseline 1713/113 is historical authoring evidence,
not the floor at a future Task 4 dispatch.

## What is preserved

- Candidate complete type module and séance action body; generation/validation/attachment
  algorithm; exact consumer map; remaining test and chunk requirements; scope and licenses.
- Departed remains separate historical metadata, optional/lazy at runtime; living witness
  list and cast/graph untouched. New `gen:departed` stream, unused name selection with
  stable fallback, truthful seven-field claim data and real subject-edge clue.
- Runtime binds to the claim minted in the existing secret loop; no extra claim/belief.
- Local offered-frame action, 20 coin, [00:00,04:00), once per campaign; source row endpoints
  null, provenance explicit; successful session action must save/replay through real API.

## New important source defect and accepted direction

The old risk assertion cannot pass honestly. `noticedByObserver` rejects presence;
`ingestEnemyItem` has no presence evidence arm; `recordAndIngest` skips enemy capture on
silent ticks; player-side presence specifically labels watch occupations. None of that
is a guard's evidence of an avatar entering a chapel. A spoken field-report presence
envelope alone is retained but currently has no sketch/exposure effect.

Root chose a mechanically consequential repair: narrow actual avatar chapel-night
encounter, independent of séance/hidden state; real raw observation and remote physical
report; direct spymaster sighting must have real physical provenance without invented
speech; smallest physical night-visit EvidenceEntry/ref/chronicle path, no redundant
enemy ledger; dedicated pure digest feature deduped per(kind,subject), exposure +1,
no carrier-profile identification. Root will put this R7 amendment in the HTML docket.
The draft explicitly distinguishes this scoped avatar law from general NPC anomaly gossip
and names its deliberate no-magic ordinary-night-visit behavioral delta.

## Coordination and consumers

Agreed with `/root/author_scrying`:

- `IntelEntry.provenance?: MagicProvenance {kind:'magic';spell:'scrying'|'seance';operation}`;
  `via` stays string. `sourceOf/sourceKey/sourceLabel/isMagic/singleInformantChannel`
  in `src/intel/provenance.ts` distinguish spell from NPC named seance.
- Séance emits via seance + explicit provenance, utterance with null endpoints plus one
  edge-read. Task 3 guards routeOf endpoints and Codex received endpoints; genuine living
  scry observations retain trait deductions, with no extra informant channel count.
- Task 3 magic state stays scry-only. Task 4 proposes separate `seance.ts`, `magic-types.ts`,
  `WorldState.departed?`, `WorldState.seanceUsed?`, ChronicleEntry += SeanceRecord.
- Task 3 captureEvidence guard adds residue-present OR; preserve it. Separate narrow
  `captureNightVisits` can handle silent presence without broadly running captureEvidence.
- **Late response saved:** Task 3 partial draft section 3C proposes ResidueEvidenceData
  `{id,witness,observedAt}`, ResidueReceipt `{tick,observer,messageId}`, a physical evidence
  arm with null speaker/addressee/claim fields and optional receipt, and optional residue
  data on SketchEvidenceRef. Direct evidence names the true witness; delayed evidence
  separately names the actual principal receipt. Its fair-cop audit/tests are unfinished;
  the predecessor chain is explicitly uncertified. See Task 4 draft's shared-seam section.

Production seams identified: world/types, gen, validate, attach; proposed world/departed;
sim/types, rules, campaign, proposed seance/magic-types/night-visit helper; content/economy
and terms; app loop/session and input/actions; phases, counterintel/field-reports and pure
enemy/state/digest for the approved risk repair. All need exact bounded license after
the completed source patches are authored. No composer UI is part of Task 4.

Task 2 compatibility: omit the document marker on séance. Preserve all existing conditional
Task 2 marker projections when touching reports; magic testimony is not a document viewing.

## Work required at resume

1. Read Task 3's finished draft and exact physical/provenance APIs. Resolve shared union/ref
   shapes and guards in the Task 4 draft; do not guess a future base hash.
2. Write exact generation helper, validation, attachment, campaign/session/terms/economy
   splices. Verify the candidate action against actual canAfford/debitCoin signatures.
3. Write the complete night-visit observation -> held/report -> physical evidence -> pure
   digest mechanism and direct/remote fair-cop references; inspect all union consumers.
4. Write every test body. Existing draft contains requirements only: generation/null/reroll,
   living-only, alias/serialization, atomic action walls, frozen-frame twins, provenance,
   genuine direct/delayed physical reporting and consequence, no-observer/daytime/omission,
   lazy no-magic and real live=session replay tests, complete soak/MC comparison.
5. Run author self-review and syntax/API checks; controller review, source re-verification,
   refreshed suite floor, and independent review before any implementation dispatch.

The partial draft is deliberately marked throughout. No successful validation, complete
code coverage, or implementation approval is claimed. Final task commit subject is retained:
`feat: séance — the dead speak once, at night, for a price`.
