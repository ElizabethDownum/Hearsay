# Task 7A author report — complete proposal, not implementation

Root Codex (GPT-6), 5 September2026. Authoring source d79e9ff; game/test source dc114da.
No production/test path changed. All files stayed inside Hearsay. The completed plan
contains exact renderer/registry edits and a612-line public-name/UI/test patch,
file ownership, chunk arithmetic, gates, defaults and two adversarial author passes.
Independent plan review and actual Task4-source reconciliation remain mandatory.

## Authored scope and measured probes

- All24 predicate templates; pure renderClaim takes exactly seven received fields.
- Literal registry keys require a template at compile time. The public Record
  facade remains compatible and all predicate values equal current production.
  A virtual additional predicate creates one actual missing-template diagnostic.
- Transient public-name view, composition-root label callback, props-only board,
  directive report and planner prose. Exact structured fields remain inspectable.
-33 renderer+4 public-label+7 panel tests=44. All44 exact authored tests pass under
  native Vitest against the virtual source proposals. This is one isolated probe
  entry, not44 tests added to the actual1780-test production suite.
- All11 virtual files pass both actual compiler configurations and ESLint.
  Literal workspace npm run lint also passes with the new local probe scripts.
  No full production build/gate or independent approval is claimed.
- Chunk arithmetic:7A1 +37tests/2files;7A2 +7tests/1file, relative to the actual
  future predecessor floor. Do not invent a Task4 hash or count now.

## Decisions and author corrections

Existing public views have IDs but no stored-person-name lookup. The proposed
selector reads names only for the existing street directory; venues have no separate
name field, so their public labels get only typographic spacing. No serialized
world/enemy-map field or gameplay/knowledge rule changes. HTML R10 holds the choice.

Severity changes narrative emphasis, not confidence. Count has no unit metadata,
so prose does not invent coins, people or occasions. Place remains the place named,
even in a contradictory received account. Unnamed source stays unnamed. Unsupported
and prototype predicate keys get an explicit unfamiliar-tale fallback; raw fields
remain in the detail. No ontology expansion.

First author pass corrected raw-ID assertions to allow actual English words
stole/poisoned and kept dl grid children direct using React Fragment. Expanded
TypeScript probing initially failed with37 Windows casing diagnostics; only the
probe host changed to retain canonical casing. Original diagnostics/logs remain.
Second pass checked finite folds, unchanged action/payload/offer gating, truth-blind
props, lazy/null/zero cases, escaping, and actual-base calibration. No production
mechanic was weakened for an acceptance. A report-generation attempt also needed
explicit UTF-8 instead of Python's Windows default; no proposal changed for it.

Ask-family and coercive-recruit labels are hidden by their existing default modes.
The native SSR tests cover default-visible controls; independent review must inspect
those two additional exact label hunks. No initial mode was altered for testing.

The full-file virtual snapshots are reference/probe inputs only. Existing files
must receive additive hunks reconciled with the future Task3/4 magic predecessor,
never whole-file overwrites that discard magic/provenance controls. Task7B retains
remaining debrief words/slots and the original final Task7 commit subject.

Independent review, actual predecessor reconciliation, native RED/GREEN, landed
implementation, full final gates and independent code review all remain. No worker
dispatched, no production implementation, no push.

### native-proposal (native exit 0)

```text

 RUN  v3.2.6 C:/Users/eliza/Desktop/ClaudeFiles/hearsay

 ✓ .superpowers/sdd/task-7a-validation/entry.probe.ts (44 tests) 42ms

 Test Files  1 passed (1)
      Tests  44 passed (44)
   Start at  13:31:20
   Duration  1.43s (transform 344ms, setup 0ms, collect 911ms, tests 42ms, environment 0ms, prepare 110ms)

```

### final-preflight (native exit 0)

```text
{
  "scope": "authored renderer in memory, no production implementation",
  "virtualTypeDiagnostics": 0,
  "virtualAppTypeDiagnostics": 0,
  "futureKeyDiagnostics": 1,
  "templates": 24,
  "registryValuesUnchanged": true,
  "rendererAuthoredTests": 33,
  "uiAndLabelsAuthoredTests": 11,
  "virtualLintFiles": 11,
  "source": ".superpowers\\sdd\\task-7a-author-draft.md"
}
```

### workspace-lint (native exit 0)

```text

> hearsay@0.0.1 lint
> eslint .

```

Draft SHA-256: 4e75a99545accc5269e5ba9cb23e8c02f0ee8ea48687f2abe5b8a60c5e6aa0a7
