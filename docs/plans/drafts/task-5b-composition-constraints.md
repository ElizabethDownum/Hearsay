# Task 5B complete composition constraints

13 September 2026. Proposal-only authoring under the continued Plan 9 direction, with
reasonable bounded defaults and non-blocking judgments retained for later HTML review.

- Source is fixed ef7fa3848ac3e1e71e2490576f1af9ea4f77eb64. Preserve all 356 archive
  files and 21 original named inputs. Only explicit future 5A/T5A-A1/5A2/R16/R17 overlays
  may accompany actual Task 4/7A1 source. No moving-primary source substitution.
- Preserve every original magic 240 identity/assertion and model body, except root's
  separately approved versioned identity amendment with its own exact map/acceptance cases.
  Preserve ordinary 147, physical 198 and R24/R25/R26 conservative bounds. No migrations.
- New production ownership is exactly calendar.ts, overlay.ts, threads.ts and index.ts
  under src/sim/debrief, plus tests/debrief/composition.test.ts. Ten lower readers are
  composed rather than re-authored. No additional physical-attention module is justified.
- Gate the pure public entry before reading hidden data: absent/running scenario returns
  null. Current terminal variants preserve actual resolution or explicit inconsistency.
  Task 6 still supplies exactly one terminal composition-root branch and props-only panels.
- Observation, player receipt, enemy acquisition and digest dates are separate. Preserve
  board append order, real gaps/ties, unknown dates and orphan records. Never backdate
  future reports/features or invent an event at zero to replace a missing timestamp.
- Reuse semantic attention and exact feature links. Signal keys are not feature IDs.
  Real work can have no feature; reported HQ bookkeeping is not actual work. Deduplicate
  repeated views of one act. Missing/unproved data is not proof of a phantom.
- Distinct (kind, subject) groups are descriptive historical keys, not a current-roster
  exposure score or identification countdown. Identification uses actual retained avatar
  carrier-profile subject and digest day. No pressure/threshold gameplay change.
- Preserve literal actual report stages, omitted/unknown roots, speakers and changes.
  Root identity and received copy remain separate; no content/time/venue causal guess.
  Physical sightings/acquisition do not prove caster identity, ritual or staffed attention.
- Player cards are detached terminal-current ungraded annotations, outside historical
  reconstruction (root's R28 default). HQ ledger is current reported bookkeeping with
  unrecorded update timing. Do not add an edit log or new world fields to fill those gaps.
- Every returned nested array/object is owned. No RNG, world, rule, clock, seed, threshold,
  recording, session, action-log, UI, app or configuration mutation. Persistence stays
  seed plus log with exact replay; composition itself is not a serialized save field.
- Author writes only the three named markdown outputs and owned validation tree. The
  immutable snapshot starts under excluded node_modules; use virtual complete bodies.
  No children, packages, network, services, staging, commits, pushes or shared-AI edits.
- Preserve native binary stdout/stderr/exits, argv/cwd, exact maps and all failures.
  Both actual compiler configs, scoped lint and four law diagnostics at each changed
  model path must pass. A loader failure is not behavioral RED. Use pwsh/python -X utf8.
- The complete plan names every dependency/chunk/body/test/subject and appropriate gates.
  Approved recording/watch predecessors are separate tasks. Root owns index/serial writer
  order. The original Task 5 final subject is reserved for complete integrated models.
- Two adversarial self-passes do not replace fresh independent proposal review or
  accumulated committed-code review. No complete Task 6/7B/8, UI or Plan 9 closure claim.
