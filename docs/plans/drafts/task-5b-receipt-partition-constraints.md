# Task 5B current-feature receipt correction constraints

13 September 2026. Root-authorized bounded proposal correction using canonical author
instructions; explicit timing clearance was received before native heavy gates.

- Own only the three named task-5b-receipt-partition markdown artifacts and its
  validation tree in hearsay-debrief-receipt-author-20260913. Production is read-only
  even in this host. Root owns docs/index/commits; UI author and code reviewers are
  active. No children or changes to another agent's artifacts.
- Actual base ef7fa3848ac3e1e71e2490576f1af9ea4f77eb64; all 356 archived files remain
  unchanged. All 576 prior author entries, 97 original review entries and 360 review
  supplement entries remain frozen (14 overlap, 443 unique review files).
- Exactly proposed threads.ts and additive composition.test.ts differ. Preserve all
  other 32 bodies, particularly the independently closed index.ts/R31 validator.
  Five chunks remain; maps 5B1–5B4 stay byte-identical and only two 5B5 bodies change.
- A current feature with a separate future duplicate receipt stays current with all
  future indexes removed. Preserve unique current association/attention when only a
  later duplicate is removed; never derive a new causal join from clipping support.
  If clipping removes support, retain honest unrecorded resolution as documented.
- Preserve future history in the existing beyondClock arrays. The complete original
  FeatureLink there is later receipt context and may share a current feature's ID;
  its presence does not imply future feature creation. No public shape change.
- Preserve unknown-time competitors, index identity/order, raw reference metadata,
  pure future features, inclusive boundaries, physical receipts, detached ownership,
  purity and every-operation/orphan reachability. No lower-reader redesign or broad
  damaged-history guarantees, schema, recording, gameplay, UI or new public exports.
- Preserve all 313 original cases/assertions/bytes except additive imports/appended
  cases. Add the exact reviewer fixture and seven controls: measured 321 total,
  migrated zero. Exact mixed RED/GREEN and original five reviewer controls remain
  native evidence. Both real compilers and scoped 34-body lint pass; four mandatory
  laws fire at the changed model's actual path.
- Record binary stdout/stderr/exit, actual argv/cwd/executable, source maps and copied
  compiler/lint diagnostics before formatting. Freeze labels and prior hosts/caches.
  The 25 original fixed inputs and three later supplemental inputs use separate maps.
  Seal every owned non-cache archive/helper input and all reports; exclude only the
  inventory itself and owned disposable .vite-* directories.
- No package install, external/shared-AI write, service, ACL/config change, production
  installation, staging, commit or push. Narrow native archive-read escalation only.
  No full-game/build/soak/Monte Carlo or actual UI certification is claimed by this
  pure proposal. Independent re-review, actual predecessor reconciliation and later
  integration gates remain required; Plan 10 stays outside scope.
