# Task5B ordinary feature-link completion — binding constraints

Authored by Codex GPT-6 at inherited highest controller capability,5 September2026.
Proposal fragment only. The complete Task5B model and production integration remain
unfinished; independent proposal review and actual-predecessor reconciliation are required.

- Authority: docs/design-spec.md:79–89, task-5b-model-premises.md, Plan11 constraints,
  the completion-author brief, the approved R20 received-document reference, and
  task-5b-incomplete-batch-adjudication-2026-09-05.md (PROVISIONAL local R24).
- “Actual attention and the features that resulted are separate facts”; do not
  substitute coincidence, the same words, family/target overlap, issued orders,
  returned headquarters claims, or final mutable carried content for a retained link.
- Keep R20's received network-speech reference. Only this postgame model may follow
  complete retained per-item roots to the original raw observation and witness.
  A direct document observation retains its claim reference. No live knowledge changes.
- Reference resolution distinguishes resolved, ambiguous, unrecorded, late, missing
  and unsupported. Attention resolution is separate: linked, ambiguous, unrecorded,
  missing, none, unsupported. “None” means no proved attention association for a
  uniquely retained event; it never means that no unrecorded work occurred.
- A multi-item receipt may associate a forged-document/runaround feature with an
  earlier observation only when the received marked-answer candidate is unique for
  that subject and its root association is complete and unique. Distractors confer
  no links. Multiple matching candidates stay ambiguous; do not pick the first.
- Raw holdings identify original witnesses. Reported-only holdings are not raw
  truth. Require one actual physical event with that witness. Duplicate physical
  events or same-beat questioning causes remain explicit ambiguity.
- Work history proves a recorded staffing beat, not uninterrupted staffing. Link
  a watch only to an actually heard observation on a retained worked beat at that
  guard/post; a later observation at the same venue does not prove watch work.
- After a corroborated but incomplete ordinary report batch, reserve only matching
  child-projection candidates within its maximum original append span. Keep their
  arrival unrecorded/null; do not search beyond the span or reserve unrelated rows.
  Byte-indistinguishable direct/report candidates remain unknown. This is R24's
  exact bounded proposal correction, not new runtime state or acquisition physics.
- Preserve recorded digest day independently from feature.day, observation day and
  arrival day. Unknown receipts and orphan features never backdate into a calendar.
- All returned nested values/arrays are owned copies. Reading the model must not
  mutate the world, hypotheses, live actor state, seeds, thresholds, rules or clocks.
- Preserve every original96 test identity and assertion and the seven unchanged
  model modules. Only the supporting evidence proposal is extended. Source basis
  is committed09e54582f0a5fc9505e396bd1910c800c7413fff plus the exact frozen26-file
  input map and explicitly documented Task5A/5A2/R16/R17/provenance overlays.
- Dedicated physical magic references remain explicitly unsupported here. Before
  full Task5B dispatch, separately author/review the actual magic operations,
  physical evidence/receipt branches, story/séance integration, calendar and
  terminal composition against implemented predecessors. Do not claim this fragment
  makes their future widened unions or full model implementation compile.
- Do not edit root/source/app/test/config/index files during author validation.
  Snapshot copies begin under the owned validation/node_modules directory; tests
  execute only through the isolated .probe.ts loader. Historical proof is immutable.
- No new packages, installs, assets, external calls, children, commits or pushes.
- Layered-config law: any future replace-not-merge configuration change must carry
  every applicable law in every matching scope and prove each with a firing test.
  This proposal changes no repository config; the validation runner reuses the
  exact committed config and proves its determinism/import rules fire at both paths.
- Model assignment: frontier_implementer for the atomic causal fold and tests;
  independent frontier_reviewer or higher, never the author, for proposal and code.
  Root owns the index and serial production writers. A mechanical_worker may later
  transcribe only hash-identical reviewed artifacts with integrity/type/test gates.
- Emergent-behavior assertions are hypotheses. Failing acceptance → STOP and report
  evidence; never weaken thresholds/formulas/physics/seeds or edit the spec.
  An untraceable existing-suite failure → BLOCKED. Plan-mandated defects count at
  full severity. No source mismatch is permission to overwrite a future predecessor.
- Author evidence: frozen96 native baseline; final WIP RED122 pass/22 fail;
  corrected144/144 (96+48), both actual compiler configurations and virtual lint0.
  Future production gate: npm test; npm run lint; npm run typecheck; npm run app:build.
  Soak/MC join only for integration or runtime changes required by the full plan.
