# Task 5B magic story identity correction author report

13 September 2026. Complete bounded replacement, ready for independent re-review.
No production source, test, configuration, index or commit was modified. The author
used the canonical author skill and inherited root capability; no child was launched.

## Correction and result

The exact keyed historical claim now remains the event lookup's authority. A second
object that repeats its internal ID stays visible in the existing version display
but cannot overwrite the canonical lookup entry. This prevents a valid séance from
moving into an unrelated family. The correction is one guarded insertion and an
explanatory comment in stories.ts. The only other target is magic.test.ts: one import
and one appended permanent regression. All original test bytes are mechanically
recoverable by deleting only those additions; all 240 case identities and assertions
are retained. No missing/ID-inconsistent root logic or magic reader changed.

| Corrected target | SHA-256 | Bytes |
| --- | --- | --- |
| `src/sim/debrief/stories.ts` | `3ea92f3bd6253a5b9a907a6925fbac39b80da887c197ca9767577fca87495eb2` | 7684 |
| `tests/debrief/magic.test.ts` | `a324a91c2ec7dfb5f1d9623f6e2f8804d44badc0d562ad676067f35b8817699c` | 21930 |

The draft carries the two complete corrected bodies, dependency contract, scope,
execution/reconciliation instructions and acceptance criteria. The corrected complete
29-body map is `task-5b-magic-identity-validation/green/corrected-241.source-map.json`.
Its hash is `3abe935e6e11e3d28cec354bc94a0d0435cfc1be3511b4fca5be93d717812def`.
The 27 other map bodies are unchanged. This is a replacement proposal, not approval
of full Task 5B or an installation into production.

## Source identity and integrity

Fixed actual archive: 152f2a0d788d7166150ba31b25090f3aa21d199c, 351 files, all unchanged.
The native-owned host was created afresh in this task's excluded node_modules from
that Git archive. The virtual loader supplies the frozen 29 transitive bodies at
their real project paths; no archived source file was overwritten. Nine of those
bodies remain explicit future Task 5A/T5A-A1/5A2/R16/R17 overlays, and ten are test
bodies; the production baseline does not yet contain these debrief models.

All 533 entries of the old author's frozen inventory were independently verified
before assembly and again by the final audit. Twenty exact inputs, including original
proposal/report/constraints/map/inventory, reviewer report, reviewer probe/raw failure,
source inventory and provenance, were copied and checked. The original reviewer probe
is identical except for rebased module paths. No author/reviewer cache was consumed.
The new source inventory, case registry, archive manifest, fixed-input map and final
audit proof retain these identities. The final evidence inventory excludes only itself
and explicitly named volatile Vitest cache directories; it includes archive/source,
all immutable helper/probe bodies, raw evidence and the three final markdown artifacts.

## Native verification

| Gate | Measured result |
| --- | --- |
| Original reviewer reproduction | Exit 1; 1 failed / 1 passed, exact s0 vs unrelated-family failure. |
| Unchanged frozen suite in fresh host | Exit 0; 240 passed. |
| Permanent 241-case RED, frozen stories | Exit 1; 240 passed / 1 failed, only the added alias case. |
| Permanent 241-case GREEN, corrected stories | Exit 0; 241 passed. |
| Original reviewer controls after correction | Exit 0; 2 passed. |
| Author adjacency controls | Exit 0; 4 passed. |
| Actual tsconfig.json and tsconfig.app.json | Native exit 0; zero diagnostics in each. |
| Scoped lint | Native exit 0; 29 virtual paths, zero errors/warnings. |
| Mandatory actual-path firing | Native exit 0; four required diagnostics at each of magic.ts and stories.ts. |
| Final integrity audit | Native exit 0; 533 old entries, 20 inputs, 351 archive files, 29 bodies, original assertions and case arithmetic verified. |

Exactly 241 = 240 unchanged original cases + 1 new case. MIGRATED: none. Supplemental
reviewer and adjacency controls are separately counted and do not inflate 241.
Every test run records native command/cwd, binary stdout/stderr, exit, executed source
map and preservation result before console presentation. The final audit verifies
the old case names as a multiset, not just a numeric total; every old case passes in
the permanent RED as well as GREEN. RED-to-GREEN differs only in stories.ts.

## Two adversarial author passes

Pass 1 traced the real ritual through retained chronicle record, magicThreads' direct
claim lookup, legacy threadOf, and storyThreads' intermediate lookup. The reviewer
probe reproduced the failure without changing the valid exact root. The one-line
guard makes a later alias ineligible, independent of sorting stability. Inspection
of adjacent consumers found magicThreads and artifactThreads already use the direct
dictionary lookup; the existing parent comparison does not use the vulnerable Map.
Those readers and the existing unresolved filter remain unchanged. An alias to the
same object cannot supply a different family; a copied alias cannot pass identity.

Pass 2 reversed insertion and retention assumptions. Two controls put two malformed
aliases before and after the exact root and compare the complete canonical thread
with its pre-alias value, including ordinary retained events. Alias versions remain
detached, with no event/origin/activity attribution. Two more controls remove the
exact root or replace its internal ID while leaving an alias carrying the old ID:
neither alias repairs the identity; the ritual remains unresolved and the magic
reader keeps its missing/inconsistent state. All four pass. The permanent case also
checks the actual record's claim/tick, changes [], changedBy null, no new alias origin,
no unresolved duplicate, repeated equality and the unchanged world hash. No additional
scope or test weakening was needed.

## Setup/evidence caveats

The first preparation attempt expected an entirely empty status and stopped before
assembly because this task's three newly authored helpers were untracked. The guard
was narrowed to the explicitly owned validation prefix; tracked paths remain clean.
That precondition failure was visible in the tool result, with no source/input writes
by preparation, and is not behavioral RED.

One sandbox compiler attempt could not read the native-owned archive. It produced
469 missing-module diagnostics and then the runner's archive audit hit PermissionError.
Its raw child command/stdout/stderr/exit/source map remain saved as
`green/actual-compilers-scoped-lint.*`; this was not a native source failure. The shared
diagnostics output filename was overwritten by the subsequent successful native gate.
To avoid claiming the original diagnostics bytes were preserved, the same sandbox
read failure was reproduced in the separate `sandbox-compiler-control` stage, whose
469 diagnostics and raw files are retained independently. This later reproduction is
distinct from the original invocation. The successful native gate has its own immutable
prefix and zero-diagnostic proof. No ACL or compiler configuration changed.

## Release and limits

All owned artifacts are frozen after the final inventory; only owned Vitest cache
directories remain volatile. The report, constraints and draft are ready for root's
hash verification and independent re-review. Composition must consume this replacement
only after that review approves it. Original artifacts remain unchanged. This unit
does not promise arbitrary damaged-history recovery, add a reader/schema, or authorize
production writes, calendar/terminal/UI changes, whole-game/build/soak/Monte Carlo,
commits or pushes. Actual-base reconciliation is still required before implementation.

## Exact final native suite output

```text

 RUN  v3.2.6 C:/Users/eliza/Desktop/ClaudeFiles/hearsay/node_modules/hearsay-magic-identity-author-20260913/.superpowers/sdd/task-5b-magic-identity-validation/node_modules/hearsay-baseline

 ✓ ../../green/entry.probe.ts (241 tests) 976ms

 Test Files  1 passed (1)
      Tests  241 passed (241)
   Start at  14:59:20
   Duration  5.82s (transform 3.07s, setup 0ms, collect 4.20s, tests 976ms, environment 0ms, prepare 207ms)

JSON report written to C:/Users/eliza/Desktop/ClaudeFiles/hearsay/node_modules/hearsay-magic-identity-author-20260913/.superpowers/sdd/task-5b-magic-identity-validation/green/corrected-241.results.json
```
