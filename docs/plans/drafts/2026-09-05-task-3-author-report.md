# Task 3 author report — PARTIAL, restart handoff

Date: 2026-09-05. Native Codex author seat; inherited exact model unavailable. Root requested immediate wind-down because Ellie is restarting the machine. **Not dispatch-ready. No implementation or test execution occurred.**

Owned outputs only:
- `C:\Users\eliza\Desktop\ClaudeFiles\hearsay\.superpowers\sdd\task-3-author-draft.md`
- `C:\Users\eliza\Desktop\ClaudeFiles\hearsay\.superpowers\sdd\task-3-author-report.md`

Saved: binding constraints; source/API premises; D1–D6 choices; exact proposed production edits for live venue perception, additive magic provenance, paid next-day scry scheduling, persistent physical residue, lawful direct/remote discovery, report omission/retry, dedicated residue evidence/ref/digest, and board/web/ledger/report/codex consumers. Exact sensor, provenance, panel, action, residue and helper fixture test bodies are drafted. These are unverified authored code, not a claim that they compile.

Root explicitly accepted D1–D6 on 2026-09-05: original three scene channels; distinct scene-presence; symmetric locally visible residue captured only by existing principals/rosters; campaign-persistent traces with retry only after a later local encounter; one residue feature per venue; magic-aware physical-telling dedupe with no independent informant corroboration. Root owns the in-project review record. Do not re-ask Ellie about these decisions.

Important R5 refinement from root: report receipt never deletes the physical trace. The event projection carries only id/venue/createdAt, excluding every discovery/delivery latch, so receipt cannot become a remote disappearance oracle.

Remaining work, in order:
1. Finish the dedicated fair-cop audit branch in `tests/sim/sketch-faircop.test.ts` as exact code, retaining the old claim/network/asking branch unchanged. Match residue refs to the captured residue evidence and the actual `ResidueRecord` creation/sighting. A received residue must additionally resolve `receipt` to a real network-speech row heard by its principal and containing the exact spoken residue atom. Write positive direct-spymaster and delayed-report controls; negative wrong witness, missing sighting, wrong/missing envelope, omitted item, and unrelated existing claim/network controls. **The current draft does not yet provide this widening; it must not be implemented without it.**
2. Finish `tests/app/magic-session.test.ts` exact successful-action save/live/replay and failed-action log exclusion bodies. The staged core replay test is present, but does not replace the app session acceptance.
3. Reconcile commit units. Current 3A widens Observation with residue before all 3C consumers have behavior; its footnote admits a compile dependency but is not a final valid chunking plan. Recommended repair: keep 3A to provenance/live sensor, move the residue union/event/observer additions and residue-specific perception test into 3C; stage coherent compile-complete chunks. Do not commit temporary casts or broken exhaustive consumers.
4. Reconcile every file license and test placement. The helper and full tests are spelled in 3E but must be licensed/tests-first in the mechanism unit that needs them. Add the final gate/report comparison protocol, complete deferred-scope list and reusable-work assessment.
5. Run mandatory author-defect self-review twice (top-down and reverse order), then permitted read-only syntax/API/type probes. No author test suite is required. Review termination/retry loops, evidence provenance, optional absent keys, copied field compatibility, D6 counting semantics, and scope. Re-check all snippets against actual Task 2 committed source, including every conditional document projection.

Known review targets, not adjudicated defects:
- Sensor phase integration test assumes a staged injected citizen speaks during that tick; inspect the actual selection mechanism before accepting its nonempty assertion. The draft explicitly licenses reporting a wrong vehicle rather than changing physics.
- `ingestEnemyResidue` dedupes a trace from its first lawful direct/report entry; a delayed entry uses the original physical witness and a separate actual principal-receipt pointer. Verify the complete fair-cop chain rather than treating either alone as sufficient.
- Report retry leaves omitted residue holds undelivered and resets queuedIn only after terminal attempt plus a new local encounter. Confirm unrelated omitted atoms still follow the old completion behavior and report-of-report holds cannot duplicate indefinitely.
- No-magic world/state/report bytes are intended unchanged, but no before/after compiler or deterministic-report comparison has been performed. Unexpected measured movement remains a reportable failure; never retune.

Task 4 coordination already sent to root and author_seance:
- `IntelEntry.provenance?: MagicProvenance`, where `{ kind:'magic'; spell:'scrying'|'seance'; operation:string }`; legacy via string remains. Helpers `sourceOf`, `sourceKey`, `sourceLabel`, `isMagic`, `singleInformantChannel` live in proposed `src/intel/provenance.ts`.
- Task 4 emits null speaker/addressee for departed testimony. Draft now makes routeOf require real endpoints and keeps magic rows with missing endpoints out of receive/emit pairing; real living scry speech still supports trait deductions.
- T3 proposes `ScryRecord` and `ResidueRecord` additions to ChronicleEntry, and WorldState.magic for scry windows/traces only. Task 4 separately owns seance.ts/magic-types.ts, RuntimeDeparted/seanceUsed and SeanceRecord; no shared magic.ts rewrite.
- T3 widens silent-tick enemy capture only when physical residue events exist. Task 4 author proposed a separate narrow `captureNightVisits(world, events)` hook, so normal NPC presence need not be broadly captured.
- Physical evidence pattern: residue EvidenceEntry arm uses Omit<EvidenceBase,'speaker'|'addressedTo'> plus null endpoints, no claim/family/reported/about, residue `{id,witness,observedAt}`, optional receipt `{tick,observer,messageId}`; old arms gain optional-never discriminants. SketchEvidenceRef gains optional residue data; old refs remain byte-identical. This is a proposed pattern pending the missing fair-cop authoring, not a certified abstraction to copy unchecked.

Observed repository context: the initial author read was during Task 1 recovery; root later reported Task 1 recovery at `caa5a38` with independent review running. Task 2 had not supplied an implementation base to this author. Do not guess a future hash. Root must verify prerequisites and exact committed HEAD when resuming.

Verification performed: source reads, current plan/audit/review/constraints inspection, and writing the two assigned documents only. No production/test file, git index, ledger, review HTML, original plan, AI repository, package, network, process-control, or subagent changes. No completed author self-review is claimed.
