# Task 7A correction — binding author/implementation constraints

- This corrects only the measured prose I1/M1 findings. Original proposal SHA256 is `4E75A99545ACCC5269E5BA9CB23E8C02F0EE8EA48687F2ABE5B8A60C5E6AA0A7`; original independent verdict remains Needs-fixes.
- Preserve all 24 predicate values, seven received fields, public `Record` facade, structural template completeness, null/zero/SOMEONE/unknown handling, action/offer/payload behavior and all 44 prior authored tests.
- Fixed predicates retain their full clause under object-only changes. Where the predicate does not specify the extra person's role, use the existing neutral account-naming phrase. Explicit relational predicates retain their arguments.
- Keep `NameOf(id): string` and the public `you` avatar label. Grammar derives only from that explicit returned pronoun; no identity, mind, relationship, schedule or knowledge lookup enters the renderer. Subject, object qualifier and attribution must agree with `you`.
- No new mechanic, dependency, hidden read, serialized field, UI control-mode change, source/test/config installation, commit, push or child agent is authorized by authoring.
- Corrected artifacts are versioned separately; original draft/report/manifests/probes/review evidence stay unchanged. The two R20-owned source files are not part of the correction.
- Actual approved Task 4 predecessor is unknown. Implementation must reconcile additive hunks and preserve magic/provenance/medium/vocabulary; never install complete old-base snapshots.
- New exact regression tests require native virtual RED against the original proposal, GREEN against the correction, both compiler configurations, virtual lint, registry value equality and missing-template firing. These are planning evidence only.
- Actual implementation gate remains `npm test`, `npm run lint`, `npm run typecheck`, `npm run app:build`; any actual semantic/world change requires the applicable simulation gates. No existing test retirement is authorized.
- Emergent-behavior assertions are hypotheses. Failing acceptance -> STOP and report with evidence; never weaken thresholds/formulas/physics/seeds or edit the spec. Untraceable existing-suite failure -> BLOCKED.
- No config law changes are planned. If replace-not-merge config becomes necessary, every scoped block must retain every applicable law and each law needs an executed firing proof; stop and obtain a scoped amendment before such a change.
- No external calls, installs, process control, UI automation, third-party reflection loading or malware-like techniques. Anything antivirus-adjacent -> stop/report. Exact native escalation is allowed for the known loader boundary; no permission/config workaround.
