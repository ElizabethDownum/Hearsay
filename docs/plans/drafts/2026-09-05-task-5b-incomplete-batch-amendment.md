# Task 5B local proposal correction — incomplete report chronology

PROVISIONAL controller adjudication, 5 September 2026. Proposal only; no gameplay
code or recording interface is changed. This applies solely to the new feature-link
completion author's supporting evidence fold. Older 96-case fragments stay frozen.

The reproduced actual one-hop vehicle in
task-5b-feature-links-completion-validation/feature-links-test-body.txt:240–249
removes one retained child from a real two-item report. The surviving report row
is then classified as direct at tick 1980 because its relay also heard the original.
Raw failure: green/author-pass1-native.stderr.log under the same validation root.
The original heard time does not establish when headquarters acquired this row.

Authority: completion-author brief lines 38–54 requires distinct missing/undated
and received/direct states and prohibits substituting content coincidence for a
retained causal link. Committed 09e counterintel.ts:66–82 appends a corroborated
received envelope before ingestion; directives/field-reports.ts:371–420 appends
at most one evidence row per projected item, synchronously and consecutively.
The maximum ordinary child span therefore follows from the actual append mechanism.
This is an ordinary legacy-speech fold; physical magic remains explicitly deferred.

Adopt the author's exact pending algorithm in
task-5b-feature-links-completion-validation/reservation-proposal.txt, subject to
its tests and independent review. After a corroborated envelope has an incomplete
child batch, reserve only subsequent rows inside that maximum span whose complete
existing projection key equals a possible child. Those rows retain the existing
unrecorded/null-arrival result and cannot enter the direct fallback. Do not reserve
unrelated rows or search beyond that bounded span. Complete batches keep their
current exact receipt/root behavior.

Required controls: the reproduced partial one-hop row stays unrecorded; a separately
captured direct observation with a distinguishable observer/content key inside the
vacated span remains direct; a byte-indistinguishable direct/report candidate in a
broken span remains unknown. The latter is an explicit information limit, not a
claim that it can be resolved. Preserve every earlier 96 assertion, complete-report
and multi-item controls, ownership and chronology distinctions. Retain failed and
corrected native evidence, both compilers/lint and exact proposal equality.

This decision authorizes a bounded proposal amendment only. Any broader repair,
new retained state or changed runtime acquisition rule requires separate scope.
Independent proposal review and eventual actual-predecessor code review remain.
