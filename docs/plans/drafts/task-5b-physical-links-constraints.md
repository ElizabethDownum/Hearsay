# Task 5B physical extension constraints

13 September 2026. Proposal only. Current user continuation overrides the September 6
session pause. Governing inputs: the physical author brief, design-spec debrief laws,
Plan 11 constraints, model premises, ordinary completion/correction constraints and R24/R25.

- Fixed tracked base aaaa57bd6bea2ce11414440f8c702173b53fb4b4; all future overlays are named
  in the draft and executable assembly. Never copy a moving primary source over this base.
- Actual direct principal observation and later actual remote speech are different channels.
  Require marked exact identities, recorded sightings and heard atoms. No held-content,
  intended-route, envelope-only, same-null-ID, later-trace or caster inference.
- Preserve observation, acquisition and digest day independently. Missing, ambiguous,
  unsupported, late and orphan facts remain explicit. Missing data is not negative truth.
- Residue has no caster subject; chapel presence does not establish séance causation.
  No séance-to-human-speech, belief, carrier, questioning or staffed-watch invention.
- Mixed reports account for physical append/dedupe slots without promoting uncertainty.
  R24/R25 reservation stays bounded by the maximum possible append span; no recursion from
  a reserved nested envelope; no association beyond the span.
- Preserve all 147 inherited identities/assertions and eight exact test bodies, plus the
  seven unaffected model bodies. MIGRATED: none. New permanent additions: 51; total: 198.
- Own copies of every returned nested ref, feature, index and attention array. No world,
  rule, threshold, seed, clock, generation or runtime-recording mutation.
- Production scope is only evidence.ts, feature-links.ts and physical-links.test.ts as
  complete bodies in the draft. No new gameplay state. All helpers/consumers are included.
- Author writes only these three proposal/report/constraints files and the owned validation
  directory. Source/test snapshot starts under excluded node_modules; virtual loader only.
  No packages, network, children, staging, commits, pushes or shared-AI writes.
- Both actual TypeScript configs, scoped lint and actual determinism/import firing proofs
  must pass. Preserve binary stdout/stderr/native exits before formatting. Use pwsh and
  python -X utf8. A loader error is not behavioral RED.
- Do not weaken tests or gameplay to fix a failed expectation. Report concrete traces and
  retain failed evidence. Plan-mandated defects retain full severity. Independent proposal
  review and actual predecessor reconciliation remain mandatory before production dispatch.
- Remaining work: magic-operation history, additional physical-operation/attention models,
  story/séance model integration, calendar, terminal composition, UI and whole-game review.
