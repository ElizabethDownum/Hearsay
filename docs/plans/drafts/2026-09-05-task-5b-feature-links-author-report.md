# Task5B ordinary feature-link completion — author report

Authored-by: Codex GPT-6 at inherited highest controller capability,5September2026.
Status: **complete bounded proposal; independent review required**. No production
implementation, full Task5B completion, code approval, commit or push is claimed.

The complete-code draft is task-5b-feature-links-completed-author-draft.md,
SHA256 22b1b12fa0ba1dede919247bd9b93f7d044fa975290ee0af8ea0af343696b664. Its binding constraints are
task-5b-feature-links-completion-constraints.md,
SHA256 dc318d600fcb641a5b5b5bf7d86973c0213a7e938b22b7864922f40a8107b800.
Three complete code fences are byte-equal to the exact UTF8/LF runtime inputs.

## Result and scope

The new ordinary fold preserves the actual feature ref while resolving its
evidence receipt and any proved performed attention separately. R20's direct claim
refs and received network refs both work through real engine fixtures. A received
document envelope follows only one relevant marked-answer item's complete retained
root to an original raw witness and physical event. Same-envelope distractors,
competing answers, duplicate history, missing raw sources and malformed roots do
not acquire guessed attention links. A runaround copied from that document lead
retains the same receipt ref. The post-trait disclosed subject stays unchanged.

The supporting evidence extension preserves all96 established assertions and adds
optional reportEvidenceIndex/reportItemIndex/rootFingerprint only when roots form
a complete, unique, nonempty-string array. It also includes controller-adopted R24:
matching candidates inside a corroborated but broken batch's maximum append span
stay unrecorded instead of falling through to a false direct receipt date.

Nine partial source modules now have144 isolated author cases: the original eight
modules/96 cases plus48 new cases in one file. This is not a whole-game test total.
Only evidence.ts changes among the old modules. Physical magic branches, actual
séance/story integration, calendar and terminal/full-model composition remain open.

## Frozen inputs and actual validation

All work stayed in the four licensed new artifact paths/validation directory.
The snapshot began under validation/node_modules/hearsay-baseline and contains
committed09e54582f0a5fc9505e396bd1910c800c7413fff, including R20. Its302 original
files remain unchanged. There are no root source/test/config/index writes by this
author. Native execution used PowerShell Core7.6.5, Python binary subprocess output
capture, Node22.22.0 and Vitest3.2.6; no PowerShell5 shim or output recoding was used.

The exact26-file frozen map was read from task-5b-reports-validation. It supplies
the original Task5A recording, Task5A2/R16/R17 and narrow Task3 provenance overlays,
the eight partial model sources and seven original test files. These historical
full-file overlays were never written over the mutable live3B/3C checkout. The
new28-file map preserves25 original values exactly, replaces evidence.ts, and
adds feature-links.ts plus feature-links.test.ts. The manifest names every input
and hash. Task5A/T5A-A1's actual future séance integration is explicitly pending,
not represented by the older speech-only overlay.

| Gate | Actual result | Raw evidence under the validation directory |
| --- | --- | --- |
| Frozen baseline |96/96, exit0 | baseline/native-tests.* |
| Baseline compilers/lint | both configs0, lint0 | baseline/compiler-lint.*, proof.json |
| Final frozen-WIP RED |122 passed/22 failed, exit1 | red/final-red-native.* |
| Corrected proposal GREEN |144/144, exit0 | green/final-green-native.* |
| Final actual compiler configs/lint | both configs0, lint0 | green/final-preflight.*, proof.json, diagnostics/lint JSON |
| Actual scoped lint firing |4 required diagnostics per proposed source path | lint-firing-proof.json |
| Exact input/fence/history integrity |302 snapshot files,193 old artifacts,25 original values unchanged;3 exact fences | integrity.json, historical-before/after.json, snapshot-manifest.json |

All stdout/stderr and exit files are native raw bytes, with command/cwd invocation
JSON. Final test JSON contains all144 measured case identities. One sandbox loader
attempt failed to read an ancestor directory; its baseline/tests.* evidence remains.
Exact licensed native escalation ran the same isolated command successfully.
The final RED tests use the original WIP/evidence extension; the runner makes only
the stated nonsemantic nullable-day concat typing adjustment in the RED vehicle.
The final green includes the actual approved R24 amendment. No missing import or
loader failure is counted as mechanism RED.

## WIP reconciliation, by unit

| WIP unit | Disposition and basis |
| --- | --- |
| Public FeatureLink/ref shapes | KEEP owned feature/ref/index output; COMPLETE with separate attentionResolution and explicit late status. Arrival resolution is not attention proof. |
| directSource | KEEP actual event identity and compartment-listener semantics; missing claim is explicit unrecorded. No similar-content recovery. |
| sourceOf/root recovery | CORRECT R20's envelope path: exactly one relevant received document answer and its complete parent/item/root association. KEEP raw-only original-witness recovery and dedupe identical raw holdings; conflicting sources stay ambiguous. |
| eventIndexes | KEEP exact tick/venue/speaker/addressee/claim-or-message plus original witness. CORRECT network copy equality and type narrowing before reading venue. |
| attentionFor questioning/compulsion | KEEP actual same-beat event relationships; CORRECT duplicate same-beat causes to explicit ambiguity. Actual public answer is questioning only; actual invitational interrogation supplies both asking and compulsion. |
| attentionFor watch | CORRECT WIP lines96–100, which treated every later heard observation that evening as watch work. A retained raw watch result proves its occurredAt beat only, so later coincidence confers no causal link. |
| featureLinks calendar | KEEP retained decision day instead of feature.day/observedAt; COMPLETE nullable-day typing and distinguish later-only refs from missing ones. Cap attention facts by retained day and available world tick. |
| counterFeatureLinks | KEEP actual attention separate from features and exact signal-to-act links; COMPLETE unresolved attention status without treating no feature as no work. |
| Evidence identity extension | KEEP actual envelope-first contiguous ingestion and per-item roots. CORRECT malformed array/type/hole validation and bind reportEvidenceIndex to the specific received wrapper. |
| Evidence direct fallback | CORRECT the newly reproduced R24 partial-batch backdating under the exact controller adjudication; only matching candidates in the maximum append span are reserved. |
| Original preparation script | DISCARD as executable vehicle only: it expected a nonexistent test file and targeted old mutable outputs/live source. Preserve its bytes; replace with owned isolated snapshot/map/runner scripts. |

The watch restriction is grounded in the retained outcome contract and code:
task-5b-attention-proposal.txt:55–70 groups actual watch-worked results into one
guard/venue/day episode; its matches comment explicitly disclaims that every minute
was staffed (lines86–87). Committed execution.ts:861–881 (with the exact R17 overlay) records
the first proved occupied post beat per day, not a continuous interval. The new
positive test uses a normal-phase watch result and a real heard network speech on
that same beat. The15-minute-later twin retains the original outcome unchanged and
hears real speech later; OLD WIP incorrectly links it, GREEN leaves it unlinked.

R24 is grounded in committed counterintel.ts:66–82 and field-reports.ts:371–420:
the heard envelope is appended first, then ordinary items synchronously append at
most one row each. A deleted child cannot survive beyond that maximum original
span. Complete batches retain their exact receipt behavior. The distinguishable
direct control uses a separately staged typed asking with actual captureEvidence
and addressed heardBy inside the vacated span; it is explicitly a unit capture
fixture, not claimed as another full normal-phase event. Byte-indistinguishable
candidates remain unknown, and a separate negative proves no reservation beyond
the span. This correction is PROVISIONAL R24, subject to independent review.

## Failed vehicles and author corrections retained

Initial corrected-model scaffold passed130 cases. Adding real watch controls
produced134 pass/3 fixture failures: the staged guard inquiry emitted no asking or
answer on the proved work beat. No model assertion was weakened. The diagnostic
events are preserved in controls-diagnostic-native.*. The vehicle was changed to
a real separately queued network speech; its first allocation was eligible one
beat early (2415 instead of2430), shown by the actual received report history in
controls-network-native.*. Queuing at the measured actual beat fixed the vehicle;
controls-timed-native.* passes137 cases. Final later-speech control changes no
outcome timestamp. These are fixture repairs, not gameplay retuning or regressions.

Initial compiler validation found two author typing defects: the inherited
ChronicleEntry venue access preceded narrowing, and the new signal fixture lacked
IntelEntry.overheard. Both were corrected explicitly; final actual compilers pass.
The raw compiler-lint failure remains. No existing configuration was weakened.

The first adversarial pass then found R24 after140 cases passed. A real one-hop
report with one retained child deleted yielded timing:direct/learnedAt:1980 for
the surviving row merely because the relay heard the original. This was reported
and held pending controller adjudication before code changes. Raw
author-pass1-native.* is140 pass/1 fail. The direct-span control then passed while
that defect remained (reservation-control-native.*:141 pass/1 fail). The adopted
correction plus ambiguous/bounded controls passes144 cases. Those failures remain
separate from the final frozen-WIP122/22 → corrected144/0 proof.

## Two adversarial author passes

Pass1 read from the causal source outward: retained roots, original witness,
receipt projection and fallback chronology. It found and reproduced R24, obtained
the explicit controller boundary and added all required controls. The source of
the defect, not a guessed date, determines the null-arrival assertion. No other
ordinary acquisition defect was silently repaired or declared a magic solution.

Pass2 read bottom-up: ownership assertions and counter composition; reference/day
partitioning; actual event/attention matching; envelope item ambiguity; bounded
projection and old96 preservation; finally complete-code/header/count consistency.
All loops terminate on fixed retained collections, and R24's loop is bounded by
the actual projected item count. The watch vehicle's31-tick diagnostic bound is a
test observation window, not a physics threshold. The model uses existing1440-tick
days and exact retained occurredAt values; no feature-distance metric or new seed,
time, RNG, pressure or confidence rule appears. APIs/configuration come from the
committed source and installed Vitest/TypeScript/ESLint code. No TODO/TBD/placeholder
or unresolved local author decision remains in the executable ordinary fragment.

Nested feature/ref/index/attention output is detached, verified by hashWorld and
separate output snapshots. Actual attention survives removal of sketch features;
signal feature ids are obtained only through exact attention ids. Physical magic
shapes are tested as unsupported and remain a named migration requirement.
The final fence/source/test map and all historical hashes are checked mechanically.
These author passes are not independent review or self-approval.

## Handoff and remaining limits

Dispatch a fresh independent reviewer at least at frontier capability with the
complete draft, constraints, root R24 adjudication, original frozen WIP identities
and validation directory. Preserve the48 new cases and all96 old assertions.
Review must evaluate the plan-mandated causal rules at full severity, then
reconcile actual future Task3/4/5A/5A2/7A/watch predecessors before production.

This ordinary proposal does not implement magic operations, residue/night-visit
receipt branches, full séance/story membership, calendar bounds, terminal-only
UI composition or the complete Task5B plan. Dedicated physical evidence unions
on the real future base require their own migration and gates; this snapshot does
not claim they compile. No whole-game suite, build, soak or MC was rerun for this
pure isolated author fragment. Those production/full-integration gates and a fresh
committed-code review remain required by the actual execution plan.
