# Task 5B magic operations and séance story constraints

13 September 2026. Proposal only. Current continuation and non-blocking recommended
defaults govern; the September 6 session pause does not block this authorized work.

- Fixed actual source: 152f2a0d788d7166150ba31b25090f3aa21d199c. Preserve all 351 archive
  files and 20 named immutable inputs/copies. Actual Task 3/4 replaces old future Task 4C
  assumptions. Future Task 5A/T5A-A1/5A2/R16/R17 overlays are explicit and additive.
- Physical 198 is the fixed transitive source. Parent reported independent approval for
  base reconciliation with no source corrections. Preserve its original artifact bytes;
  never consume its mutable Vitest cache. Independent magic review is still required.
- Group operations only by actual spell plus explicit operation ID. Retain duplicates,
  missing and inconsistent data without content/time/venue/ordinary-via identity guesses.
- Separate scheduled window, actual capture, trace creation, observation, report receipt,
  enemy acquisition and digest reference. Reuse existing report/evidence/feature readers.
  A raw held report is not proof of spoken content; an expired paid window may be empty.
- Historical price is unrecorded. Do not infer it from current rules, economy or coin.
  Residue lacks caster identity; visible chapel presence does not identify ritual causation.
- Séance is receipt of the retained historical claim and edge. It creates no human mind,
  telling, claim version, origin, mutation, belief delivery or carrier. Its event has no
  human changedBy; missing/ID-inconsistent roots remain unresolved rather than disappearing.
- Preserve 198 inherited identities/assertions, all nine old test bodies and eight unaffected
  model bodies. Preserve the 147 ordinary floor and R24/R25/R26 conservative bounds. No
  migrations. Exactly 42 new permanent cases, for 240 total, accompany only magic/stories.
- Own every nested output object/array. No mutation of world, rules, RNG, clock, seed,
  threshold, schedule, recording, configuration or gameplay. No additional attention,
  calendar, terminal, UI or full-model composition in this unit.
- Author owns only the three named markdown artifacts and the new owned validation tree.
  Virtual source snapshot starts under excluded node_modules. No normal source/test edits,
  packages, network, children, staging, commits, push or shared-AI writes.
- Retain native binary stdout/stderr/exits and exact command/cwd/source maps before formatting.
  Both real TypeScript configs, scoped lint and all four mandatory laws at magic.ts and
  stories.ts must pass. Native loader failures never count as RED. Use pwsh/python -X utf8.
- Preserve all failed evidence and two adversarial author passes. Never weaken tests or
  gameplay to satisfy them. Actual-base reconciliation and independent proposal review
  precede production dispatch; no complete Task 5 or production approval claim is made.
