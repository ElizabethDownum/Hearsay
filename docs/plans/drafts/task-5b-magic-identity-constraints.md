# Task 5B magic story identity correction constraints

13 September 2026. Bounded proposal only, using the canonical author skill and
inherited root capability. No child agents.

- Actual source base is 152f2a0d788d7166150ba31b25090f3aa21d199c. Retain the 351-file
  archive unchanged and distinguish nine future recording/watch overlays from it.
- Preserve every artifact of the frozen 240 proposal and the independent review.
  All 533 old inventory entries, 20 named fixed inputs/copies and 29 transitive body
  identities are audited. Only stories.ts and additive magic.test.ts may differ.
- A story event uses only the exact keyed retained claim. A malformed alias with
  the same internal ID cannot replace that claim's event identity or move the
  séance into its unrelated family. Do not invent an event, origin, root, speaker,
  mutation, receipt, price, time or causal association.
- Retain the existing raw version/family display and missing/ID-inconsistent ritual
  unresolved behavior. No arbitrary corrupt-history recovery or new schema promise.
- Preserve all 240 existing identities/assertions and all nine other test bodies.
  Add one permanent real-ritual alias regression: 241 total, no migrations. RED and
  GREEN have identical test bytes. Four author controls remain supplemental probes.
- Own only the three named markdown artifacts and this new validation tree.
  Production paths in every checkout, index, commits, caches and original artifacts
  are read-only. The fresh archive and virtual maps live inside owned node_modules.
- No packages, installs, network, global/config/ACL changes, external writes or push.
  Do not touch the root's 7A2 work or the separate composition author's host.
- Keep native binary stdout/stderr/exits, argv/cwd and executed source maps. Capture
  actual compiler and lint results, all four mandatory laws at both model paths,
  causal RED/GREEN and two adversarial author passes. Preserve failed evidence.
- The sandbox cannot read the native-owned archive; use the established narrow
  escalation. A sandbox loader/read failure is not behavioral RED or a source defect.
- Full-game tests, build, soak, Monte Carlo, UI and remaining composition are outside
  this proposal. Independent re-review and actual-base reconciliation remain required.
