# Task 6 / 7B constraints

This is a complete-code UI proposal in an isolated detached checkout, not production
installation. Own only the named UI draft / report / constraints and validation tree.
All prior physical, magic, identity, composition and correction artifacts remain
immutable. No primary writes, index / commit / push, shared AI edits, dependencies,
services, browser launch, global configuration or child agent work is authorized here.

The actual source base is 252a4813e1efffd15a5f08936bd3d64e2343af67. Actual 5A1 recording
and approved 7A2 prose supersede obsolete virtual bodies. Only named future R16 / R17 /
5A2 directive prerequisites remain virtual. Their installation belongs to their own
units; the UI has exactly 18 production targets in three disjoint serial units.

The original 278-case model is a preserved historical input. Final UI certification
requires the exact independently approved versioned receipt-partition correction,
with all 278 original cases, the first contract's 35 additions and the receipt unit's
eight additive cases retained. The approved model has 321 cases and the final UI
suite has 649 cases. Exact model approval is recorded in release-bindings.json and
the source / authority hash table in the author report. The rejected 313 input and provisional 638 UI evidence stay
frozen. UI code may consume additive
operations.beyondClock; it may not modify models for display convenience. Any later
model correction requires a new named reconciliation and refreshed affected checks.

One actual terminal branch calls debriefView once and constructs the sole terminal
payload. Running / scenario-free render invokes no debrief fold. All terminal panels
are props-only, including no sim type imports. Use the existing type-only townview
seam and broad real import fence. Do not add a permissive config exception or a second
weaker model entry point. Existing seed / action-log save / replay, terminal advance /
offer boundaries and input actions remain unchanged. The existing global keyboard
listener returns for terminal status so it cannot consume native terminal activation.

Keep all operation families, exact references, unresolved roots, missing receipts,
ambiguity, unknown dates and orphans reachable. Current arrays and known future arrays
stay separate; source indexes must never become positions in filtered arrays. A
current feature may also have a beyond-clock copy as context for a later receipt;
label this explicitly without implying future creation or losing the current link.
Current feature references contain no future evidence indexes. Do not
infer ritual causation from a visit, a caster from residue, an operation from orphan
physical history, or semantic attention from a physical sighting. Recorded claim
references do not automatically prove a historical ritual root. Keep all inherited
receipt-collision, batch, omission and maximum-span uncertainty rules.

Primary reading is claim prose with exact inspectable fields. Mark words only between
retained complete copies and preserve deleted-only field changes. Name a responsible
mind only with retained causal changes. Do not choose a duplicate same-ID claim version
or reindex a report root. Planned route / current packet / delivered envelope / spoken
item / acquired evidence are distinct states. Unknown work is not absent work; an
unproved signal is not proof of a phantom. Missing / inconsistent ending causes are
explicit and must not be narrated as established causal accusations.

R8 preserves performed / observed / received / acquired / digested dates. R9 shows
distinct evidence groups and actual identification, no threshold countdown. R28 keeps
terminal-current player cards ungraded, outside calendar reconstruction. R29 preserves
same-ID ambiguity. Registry / type fence / retained-data limits are binding carried
constraints. No gameplay, migration, rule, threshold or identity join changes belong
to this UI unit.

Add only the 17 named missing terms and four registered null slots; retain the existing
debrief meaning, five action terms, scrying slot and every other registry value. Use
primitive glyphs / flat paper. No media wiring or acquisition. All status signals need
text, not color alone. ARIA tabs use one focusable selected tab and no nested tooltip
tab stops. Content-derived labels and titles preserve registered vocabulary.

Native SSR / actual callback probes are author evidence, not an interactive browser
audit. The later browser procedure must use owned profiles, local-only transport,
actual rendering / native keyboard / pointer / focus / scrolling and saved evidence.
No browser or service is launched by this proposal. The staged witness / actual relay /
real clock-loss fixture is not Task 8's generated canary or legal two-council solution.

Run only scoped model / UI / registry checks here, both actual compiler configurations,
scoped lint and real firing controls. Honor root timing windows; no full-game / soak /
Monte Carlo author reruns. Preserve raw native failures, source maps, exact old cases,
archive bytes and source / copy integrity. Two adversarial author passes precede
independent UI proposal review; implementation still needs actual-base reconciliation,
applicable code gates, browser evidence and independent accumulated code review.

A7 ritual / artifact composers, near misses, unfired threads and counterfactual replay
remain deferred. Reserve the final complete Task 6 commit subject until all surfaces
and the one gate are installed. Plan 9 closure belongs to Task 8 and whole-branch review.
