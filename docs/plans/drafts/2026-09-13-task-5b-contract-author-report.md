# Task 5B composition contract correction author report

13 September 2026. Complete bounded replacement, ready for independent re-review.
Production, app, tests, configuration, index and commits remain untouched. All work
is confined to the root-created isolated author's proposal/evidence paths. Canonical
author instructions and inherited root capability were used; no children were launched.
Root's timing hold was observed until explicit clearance, then the native gate lane
was released promptly after completion.

## Delivered result

The final suite is 313 = 278 unchanged original cases + 35 additive cases. All five
independent regressions are permanent and pass. Current operation evidence/features
partition known future rows into additive typed beyondClock arrays while preserving
unknown rows, indexes and detached values. Resolution consistency now validates the
retained status-specific institutional and claim/member, feature/subject or capture
identity. The raw payload and existing consistency union are unchanged. No referee,
rule, threshold or current roster is used to decide the historical outcome again.

Exactly three bodies differ. The other 31 bodies are byte-identical: all 29 upstream
identity/physical/receipt/recording/test bodies plus calendar.ts and overlay.ts.

| Corrected target | SHA-256 | Bytes |
| --- | --- | --- |
| `src/sim/debrief/threads.ts` | `c03aeb1f920e89395a07d81fa8c82437f7a9f5525ff6d64697d250d7f2287ad2` | 4710 |
| `src/sim/debrief/index.ts` | `5585e10d0ec4cd75211f6361be2c60559d449070514670cce52e5e729a2103ae` | 7012 |
| `tests/debrief/composition.test.ts` | `63e6d1ae92ec53a0efa1998d04bbbd17931a0b7a760d80993379d60eb9da6428` | 39934 |

Complete executed map SHA-256: `f25076daf877b0ffba3a41bf33525c01c69ee318ea309f5bbf3a84bb069e1229` at
task-5b-contract-validation/final-green/complete-313-green.source-map.json.
The draft contains every corrected body, the API/uncertainty contract, all five
installation chunks and actual-base verification instructions. Four chunk maps are
byte-identical to the frozen composition maps; only 5B5 changes. The final map and
chunk manifest remain proposals, not production installation or UI certification.

## Acceptance and verification

313 = 278 preserved cases + 35 additive cases. The latter include all five binding
independent regressions, current/future/unknown chronology, inclusive clock boundary,
detached partitions, all four real endings, partial clock progress, missing/mismatched/
ambiguous resolution sources, historical member/guard identity independent of today's
roster, and retained network capture. No migrations or removed assertions.

The exact reviewer probe changes only import paths in the new isolated host. Its
five failures reproduce against the frozen map; its five cases pass against final
GREEN. The permanent version adapts only helper names and gives the three resolution
tuples an explicit mutable TypeScript annotation, preserving fixture values/assertions.
Original 278 test bytes are an unchanged contiguous body between one additive type
import and the appended new cases. The audit verifies case-name multisets, not counts
alone. Every old case passes in permanent RED and GREEN.

| Native gate | Result |
| --- | --- |
| Original fixed baseline | 278 passed, exit 0 |
| Original five reviewer probes | 0 passed / 5 failed, exit 1 |
| Final permanent RED against old models | 284 passed / 29 failed, exit 1 |
| Final permanent GREEN | 313 passed, exit 0 |
| Second author pass, conflicting-sketch control | 312 passed / 1 failed, exit 1; corrected in final GREEN |
| Final original reviewer probes | 5 passed, exit 0 |
| Both actual compiler configurations | Zero diagnostics, exit 0 |
| Exact-body lint | 34 files, zero errors and warnings, exit 0 |
| Mandatory law firing | Two changed model paths, four expected diagnostics each, exit 0 |
| Final integrity audit | 592 author entries, 457 reviewer entries, 24 fixed inputs, 356 archive files unchanged; exit 0 |

Recorded commands run from the isolated author checkout using PowerShell 7 and
python -X utf8. The established narrow native-read escalation reaches its native-owned
archive; no ACL, dependencies, network or project configuration is changed.

```text
python -X utf8 .superpowers/sdd/task-5b-contract-validation/run.py baseline tests original-278
python -X utf8 .superpowers/sdd/task-5b-contract-validation/run.py reviewer-red tests exact-reviewer-red
python -X utf8 .superpowers/sdd/task-5b-contract-validation/run.py final-red tests complete-313-red
python -X utf8 .superpowers/sdd/task-5b-contract-validation/run.py final-green tests complete-313-green
python -X utf8 .superpowers/sdd/task-5b-contract-validation/run.py reviewer-final tests final-reviewer-controls
python -X utf8 .superpowers/sdd/task-5b-contract-validation/run.py final-green preflight final-compiler-lint
python -X utf8 .superpowers/sdd/task-5b-contract-validation/run.py root firing final-firing
python -X utf8 .superpowers/sdd/task-5b-contract-validation/run.py root audit final-integrity
```

These prefixes are immutable. Re-review uses its own host/cache and new labels;
never rerun a cache in the original composition, identity, physical or review hosts.
Each native gate saves executable identity, argv/cwd, exact source map, separate
binary stdout/stderr, real exit and preservation proof. Compiler diagnostics/lint
outputs are copied to the immutable run prefix before any later stage may refresh
generated output. Final file-inventory.json excludes itself and only owned disposable
.vite-* cache directories, and includes all three final markdown artifacts.

## Two adversarial author passes and retained failures

Pass 1 traced Resolution's actual engine source: councilTurns/scenarioNightly's
denouncement, coronation and unmasking records, plus the phase's arrest after a
captured telling or network utterance. That inspection avoided using today's rosters
or reapplying thresholds. It also traced physicalAcquisition: an explicitly future
receipt becomes learnedAt:null at a rewound clock, so checking learnedAt alone would
leak the retained future receipt as undated current evidence. The partition therefore
checks the explicit receipt/physical marker as retained timestamps without promoting
them to proof of acquisition. All original reviewer probes reproduced 5 failed, the
original baseline passed 278, and the first additive suite passed 311.

The first compiler gate found one new synthetic network-capture fixture missing
SpokenNetworkPayload's required onwardTo:null. Its native error, exact source map and
diagnostics are retained under green/initial-compiler-lint. The fixture was made
structurally valid without changing any assertion or original test. This was a typed
fixture defect; no production schema or compiler configuration changed. The actual
caught-telling fixture remains a real action/phase ending; the network variant is
explicitly a typed retained-record control, not claimed as a real engine replay.

Pass 2 reversed identity and retention assumptions: duplicate/missing institutional
events, missing/mismatched claim/family/member turns, exact-key historical membership
with a changed current roster, partial clock progress without its retained belief,
future-only digest versus its sketch mirror, conflicting feature IDs in either source,
missing/mismatched/duplicated captured speech and a missing guard audience. It found
that the first correction excluded the current-sketch copy whenever a decision had
the same ID, hiding a conflicting same-ID value. The preserved self-review-red run
has 312 passed / 1 failed. index.ts now includes current-sketch mirrors when that ID
has retained nonfuture decision evidence and treats distinct copies as ambiguous.
It still refuses a future-only decision's sketch mirror as old proof. Final GREEN is
313 passed; both exact compiler configs and all 34-body lint diagnostics are zero.

Original 278 bytes are preserved as a contiguous test body; new material is only an
additive type import and appended cases. The final RED and GREEN tests are identical.
The registry verifies all old case-name multiplicities and all old cases passing in
both runs. Initial 311 maps/results and the one-diagnostic compiler failure remain
distinct from the final 313 evidence. Nothing was retuned or normalized to force pass.

## Integrity, unknowns and release

Final audit independently verifies all 592 original author entries and 457 reviewer
entries, 24 source/copy pairs, every one of 356 archived files, the exact 34 source
bodies, unchanged upstream bytes and five-chunk ownership/dependency correspondence.
All native checks ran against the new owned archive and cache. The current Git status
contains only this author's allowed untracked proposal files, no tracked edits.
The final evidence inventory includes complete bodies, reports, helpers, source/input
maps, exact native logs, case identities and the archived source, excluding only itself
and owned disposable .vite-* caches. No frozen upstream artifact/cache was changed.

Resolution has only a day, no retained terminal-event index, so ambiguous candidate
events cannot be chosen arbitrarily. Won member identity comes from denouncement
actors/claim IDs; historical credence is not separately reconstructed. Coronation
does not retain member actors, so partial clock progress needs its matching retained
belief snapshot; missing or changed snapshots remain inconsistent. Undated feature
identity can resolve a pointer without claiming a missing digest day. Consistency
does not certify the fairness of fabricated source data or recover arbitrary damaged
history. These are explicit limits of retained data, not a reason to change gameplay.

All artifacts are released frozen after sealing. Root and the UI author received
the additive beyondClock API early; no stale 278 model may be certified. Independent
correction review is required before the UI/model proposal consumes this replacement.
Actual predecessor reconciliation, installation gates and accumulated committed-code
review remain separate. No full-game, build, soak, Monte Carlo, UI integration or
Plan 9 closure is claimed by this author unit.

## Exact final native output

```text

 RUN  v3.2.6 C:/Users/eliza/Desktop/ClaudeFiles/hearsay/node_modules/hearsay-debrief-contract-author-20260913/.superpowers/sdd/task-5b-contract-validation/node_modules/hearsay-baseline

 ✓ ../../final-green/entry.probe.ts (313 tests) 669ms

 Test Files  1 passed (1)
      Tests  313 passed (313)
   Start at  16:11:39
   Duration  2.07s (transform 388ms, setup 0ms, collect 1.05s, tests 669ms, environment 0ms, prepare 79ms)

JSON report written to C:/Users/eliza/Desktop/ClaudeFiles/hearsay/node_modules/hearsay-debrief-contract-author-20260913/.superpowers/sdd/task-5b-contract-validation/final-green/complete-313-green.results.json
```
