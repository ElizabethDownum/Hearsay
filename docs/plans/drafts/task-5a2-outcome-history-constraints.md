# Task5A2 outcome history — constraints

- Retain raw local execution results before report omission/candor/traits. A heard
  claim remains a claim heard, not proof that its content is true.
- Use optional private DirectiveRecord.outcomes only after a real result occurs.
  Snapshot nested data and associate the actual response/report packet id or null.
- No extra messages, ids, speech fields, changed receipt projections or game decisions.
  buildDirectiveReport remains pure. Invalid route validation remains atomic.
- Preserve no-report/no-route behavior. Direct acceptance is no performed-work result;
  direct refusal is a result even if its acknowledgement omits the report.
- Live desk/app cannot read outcomes. Extend both existing named-prong tables and
  hidden-state twin test; reuse the shipped seven-form firing mechanism, no new scanner.
- Exact licensed changes: directives/types.ts, reports.ts, execution.ts;
  new tests/directives/outcome-history.test.ts and additive tests/directives/view.test.ts.
- Preserve Task3/4 magic and Task5A1 report-root fields on the actual predecessor.
  Current snapshots are virtual authoring evidence, never overwrite a future file.
- Root owns the index; all files stay inside Hearsay. No push/install/config/memory edits.
- Separate plan review and separate implemented-code review remain required.
- Run full test/lint/both-types/build/soak/MC at implementation. New metadata may
  change full hashes, but stripped paired world state and simulation reports must agree.
- Emergent assertions are hypotheses: failure → report, never weaken physics/thresholds/
  seeds. Untraceable existing-suite failure → BLOCKED; test-count drops need disposition.
