# Task 6 / 7B author report

13 September 2026. Complete-code terminal UI proposal in the root-created isolated
checkout at 252a4813e1efffd15a5f08936bd3d64e2343af67. No production source, index,
commit, dependency, browser, service or shared AI state was changed. The canonical
author workflow and inherited root capability were used; no children were launched.
The proposal still requires separate independent UI review and actual installation.

## Delivered behavior

The ended campaign opens one reading desk with Threads, Sketch timeline and Overlay.
All operation families and unresolved histories are reachable. Claims use approved
prose plus exact inspectable fields; actual retained changes identify their responsible
mind, while missing / ambiguous copies remain explicit. The calendar separates dates,
the overlay separates actual work from reported accounts and resulting features, and
current player notes remain ungraded outside historical reconstruction. Known future
evidence / physical acquisitions / feature references have their own section.

Main computes debriefView only in its actual terminal branch and passes one detached
payload to props-only panels through the existing type-only townview seam. Running
and scenario-free renders call no debrief fold. All four actual terminal endings are
exercised. Open / Back / Escape and roving tabs use real presentation handlers.
Missing / inconsistent resolution data does not get a falsely established causal
sentence. The existing global keyboard listener now returns at terminal status so it
cannot consume native Space activation on the debrief controls. Input actions, session
commands, playback guard, save and action-log replay remain unchanged.

Exactly 18 complete UI bodies are partitioned into three serial units: five registry
targets, ten panel / helper / presentation-test targets, and three final gate targets.
The draft contains every body and a later browser narrative / interaction procedure.
Seventeen missing terms and four null slots are added; old term / slot / asset values
are preserved. The existing debrief term still means compelled asset questioning.
No media is acquired or wired. A7 composers, near misses, counterfactual replay and
Task 8's separate campaign / canary integration remain outside this proposal.

## Fixed source and versioned model reconciliation

The owned snapshot contains all 365 tracked files from the exact committed source.
Actual approved 7A2 app / content and committed 5A1 recording are used. Five obsolete
virtual recording overrides were removed: four were byte-identical to actual source;
chronicle.ts differed only in comments / formatting around the identical three-kind
inject / telling / séance predicate. Its actual comments and source bytes are preserved.
Only named future R16 / R17 / 5A2 directive types / reports / execution / evaluator
remain virtual prerequisites. Those four paths are not UI installation targets.

The original composition input remains frozen: all 592 inventory entries and its
278 test identities are preserved. The first, rejected contract correction replaces
only threads.ts, index.ts and additive composition.test.ts. All 26 other integrated
bodies remain exact, including calendar / overlay and every other predecessor reader.
The original composition test body is retained contiguously; the correction adds 35
cases. All 576 correction-author inventory entries were independently checked here.
The correction source map hash is
f25076daf877b0ffba3a41bf33525c01c69ee318ea309f5bbf3a84bb069e1229.
Its independent review closed the ending validator finding but found that a later
duplicate receipt could remove an already supported current feature. This rejected
313 input and all provisional 638 UI evidence remain preserved, including eight
separately copied / hashed map, draft, constraints and inventory artifacts.

The separately reviewed receipt-partition replacement changes only threads.ts and
additive composition.test.ts. The ending validator and all other 32 full-map bodies
remain exact; the integrated actual-5A1 host preserves its other 27 bodies. A current
feature retains a projected current / unknown link with no future evidence indexes,
while the complete link remains reachable as later receipt context in beyondClock.
The UI explains why a feature may appear in both sections without a second creation.
Exact replacement hashes, independent authority and final case count are bound below
before release. No prior cache
is used as an input or executed. The original 278 baseline evidence is never overwritten.

## Native results and arithmetic

649 = 321 inherited model cases + 41 new UI cases + 287 cases from the existing app
suites evaluated on the final source / registries. The model cases preserve 278 original
cases, the first correction's 35 additions and the receipt replacement's additive
controls. The 287 app cases are 256 existing jargon
pipeline assertions, 24 existing asset assertions and seven existing claim-prose
assertions; the source-driven jargon case list naturally includes the new terminal
references. They are not claimed as 287 newly authored UI tests.

| Native evidence | Result |
| --- | --- |
| Original actual-source baseline | 278 passed; exit 0 |
| Original baseline compilers / scoped lint | Both configurations clear; 29 linted bodies clear |
| Corrected actual-source baseline | 313 passed; exit 0 |
| Approved receipt-partition baseline | 321 passed; exit 0 |
| First 321-bound UI attempt (preserved fixture-count failure) | 648 passed / one failed; exit 1 |
| Complete terminal UI GREEN | 649 passed; exit 0 |
| Identical permanent tests, terminal public entries disabled | 640 passed / 9 failed; exit 1 |
| Keyboard self-review RED | One failed / 599 skipped; actual preventDefault called on terminal Space |
| Keyboard guard / tab / uncertain-ending focused GREEN | Four passed / 597 skipped; exit 0 |
| Future feature / rewound physical presentation controls | Two passed / 636 skipped; exit 0 |
| Both actual compiler configurations | Zero diagnostics; exit 0 |
| Exact final TypeScript / TSX body lint | 46 files, zero errors and warnings |
| Real broad panel fence firing | Five actual panel paths; forbidden value and type imports rejected; real source clear |
| Terminal source-law firing | Real guarded call / consumer pass; hoisted call and extra alias rejected |
| Registry / asset / old prose gates | Included in complete 649-case GREEN |
| Separate author SSR capture | One passed; all three markup surfaces and campaign data saved |

Causal RED preserves every permanent test body and lexical registry reference. Only
the real public terminal entries return the old resolved placeholder before the new
flow. Nine entry / navigation / uncertain-ending checks fail; all 321 model cases
still pass. Import, registry, running non-reachability and direct reading checks stay
green. The source checker alone correctly cannot prove reachability past an early
return; the actual App / handler behavior checks supply that evidence.

The 41 new permanent UI cases cover the real losing campaign, actual navigation,
native callback semantics, all four actual endings, running non-reachability, missing
and inconsistent causes, duplicate claim candidates, missing parent and deleted-only
changes, escaping / detached notes, actual paper viewing, delayed physical receipt,
séance versus chapel sighting, omitted / unassociated report items, delayed / missing
human receipts, expired no-report watch work, missing issued / received work, known
future partitions, actual fence / gate firing, and registry / fallback integrity.
They also cover current-feature / later-duplicate readability and primary prose for
actual received copies plus typed retained headquarters / directive report copies.

Native commands run from the isolated author checkout. The owned archive / virtual
host uses existing ancestor dependencies. Only the previously authorized narrow native
test escalation is used for esbuild; there are no installs or ACL / config changes.

```text
python -X utf8 .superpowers/sdd/task-6-7b-validation/run.py baseline tests baseline-278
python -X utf8 .superpowers/sdd/task-6-7b-validation/run.py baseline-corrected tests baseline-313
python -X utf8 .superpowers/sdd/task-6-7b-validation/run.py receipt-baseline tests approved-model-baseline
python -X utf8 .superpowers/sdd/task-6-7b-validation/run.py receipt-green tests complete-ui-green-two
python -X utf8 .superpowers/sdd/task-6-7b-validation/run.py receipt-red tests complete-ui-red
python -X utf8 .superpowers/sdd/task-6-7b-validation/run.py receipt-green preflight final-compiler-lint
python -X utf8 .superpowers/sdd/task-6-7b-validation/run.py receipt-capture tests native-render-capture
python -X utf8 .superpowers/sdd/task-6-7b-validation/run.py root audit final-integrity
```

Run prefixes are immutable; reviewers use their own host and new labels. Each test /
compiler gate keeps native argv / cwd, exact source map, native configs, raw separate
stdout / stderr, real exit and archive / input preservation. Compiler diagnostic and
lint files are copied to their run prefix before any later host refresh. Final runtime
identities record Node v22.22.0, Python 3.12.10, React 19.2.7, Vitest 3.2.6, TypeScript
5.9.3, ESLint 9.39.4 and their actual paths / hashes. Project configuration is unchanged.

## Two adversarial author passes and retained failures

Pass 1 checked actual 5A1 source reconciliation, every DebriefView section, the broader
panel / type-barrel fence and actual main composition. It retained alias candidates,
unknown parent comparisons, report omissions, magic / physical separation, undated
cards and sparse calendar facts. Initial source compilation found two concrete UI
typing errors: deliveredCopy has tick, not receivedAt, and latestRun is nullable.
The preserved source-compile-one run has both diagnostics; the final code uses tick
and optional latestRun state. No model / schema change was needed.

The initial assembly marker expected a period where the new comment had a colon.
Its direct tool failure was reproduced with the saved original assembler under
initial-assembly-reproduction, preserving a real native exit and diagnostics; the
reproduction is identified as such, not a recovered original process log. An initial
correction-copy preparation also stopped before any copy because the draft filename
was wrong; the actual directory listing resolved it to task-5b-contract-draft.md.
The successful native correction-input-preparation is preserved. Neither preparation
issue is counted as behavioral RED evidence.

Initial SSR collection lacked the actual app's automatic JSX transform, producing
557 passed / 20 failed across new UI and existing prose tests. The host was aligned
to jsx: automatic, matching tsconfig.app.json. The next run passed 573 and failed four
actual main gate probes because a relative virtual module mock did not bind main's
session import. Preserved path / factory probes demonstrate the wrong C:/app and
C:/src resolutions. The final permanent harness binds the actual session import from
the known native repository root and spies on the imported model's actual export.
It verifies the fixture identity before rendering App. The focused gate-spy run
then passed all six original gate cases. Assertions were not weakened or skipped.

The filesystem read adapter exists only in the author host: actual jargon / asset /
fence scans see the virtual exact bodies, including new panel filenames. Existing
test assertions and source / registry semantics remain intact. The final suite also
logs Vite's ENOENT warning for the installed TypeScript package's absent optional
typescript.js.map. The package source itself loads, all assertions pass and both
compiler diagnostics are empty. No dependency is installed or modified to silence it.

Pass 2 exercised actual artifact viewing, delayed physical acquisition, séance plus
same-place sighting, omitted / unassociated speech roots, delayed / missing human
receipts and no-report / missing watch work. It checked future partitions against
the explicit provisional correction without certifying its authority. It also traced
the real global keyboard callback: the existing handler remained registered after
terminal return and consumed Space on terminal buttons. The native self-review RED
shows preventDefault called once. The final terminal early return fixes that seam;
the same callback leaves terminal Space alone and still handles a running shortcut.
The tab labels use registered content / titles without nested focusable Term spans,
and the native markup probe asserts exactly one tab stop inside the tab list.

This pass also avoids narrating an unproved arrest cause from missing / inconsistent
resolution data. It keeps the retained ending status but qualifies its cause. These
are bounded display / event-handling corrections; no gameplay or retained schema
changes are made. Source maps for all failed native stages remain available.

After the first contract review, the additive mixed-time UI control used actual forge
/ plant / questioning evidence plus a later duplicate receipt and reproduced the
missing current feature: one failed / 638 skipped against the rejected model. It
asserts both the current reference and the separate later receipt, with explicit
wording that the repeated feature is not future creation. The approved replacement
passes the retained causal checks in the final suite. The first 321-bound integrated
run measured 648 passed / one failed out of 649: its incidental expectation of one
future evidence row was false. The actual capture path records the network report
envelope and ingests its delivered utterance item, so both rows are legitimately
beyond the clock. That exact source map and native 648 / one trace are preserved.
The final fixture identifies the message ID and delivered claim index directly,
checks that the original future reference names that index, and excludes every
future index from all current reference index arrays. Its current feature / resolved
support assertions are retained. No model or UI behavior changed for this correction.

The final reading-line audit also found that Overlay's retained claim copies and
returned headquarters / directive claims were inspectable as JSON but lacked their
primary prose. Two new controls failed against the preserved UI and passed with
DebriefClaim / DebriefReport at those exact retained-copy sites. The analogous actual
local-result, literal network-copy and magic-capture sites now share that renderer.
The real received-copy control distinguishes count four from the private count-two
original; the second is explicitly a typed retained-report presentation control.
This adds no model reader or schema. Both compilers and 46-body lint passed after
the change; all earlier 638 artifacts and failed prose traces remain intact.

## Actual rendered lesson and remaining limits

rendered-campaign-receipt/campaign.json records a real clock loss at tick 1440, day 0,
turned: [], resolutionState: consistent. Ada's actual speech at tick 15 retains count
two / severity four. Bez's actual onward speech at tick 30 has count four / severity
five. The original claim remains count two / severity four. World hash 1734303072
is identical before and after all rendered surfaces. The staged witness / engine
relay fixture is explicit: it is not a generated Task 8 canary or a legal two-council
win. It explains this loss concretely: the report reached the player and changed in
transit, while its subject was Bez and the campaign's target was Dov.

Saved card.html, threads.html, timeline.html and overlay.html are native rendered
markup. Navigation probes call the actual component callbacks; the main keyboard
probe installs the actual registered callback with owned platform stubs. No browser
or server was launched. These checks do not certify native browser focus, pointer
hit targets, scrolling, responsive layout or accessibility. The draft specifies the
later owned-profile / local-only DevTools procedure, exact three-surface narrative,
Space / Enter / arrows / Home / End / Escape checks, two viewport sizes, light / dark
themes and saved evidence. Missing local interaction tooling at implementation is
a reported browser-gate limit, never an SSR substitute.

Unrecorded historical prices, undated current headquarters bookkeeping, inconsistent
resolution causes and detached ungraded cards are retained-data limits. The UI
explains them without claiming missing events or grading notes against feature IDs.
All further model / schema decisions remain with their separate author / review units.
No full-game / build / soak / Monte Carlo or final Plan 9 closure is claimed here.

Final integrity and exact model authority are bound below. Separate independent UI
proposal review remains required; no UI production installation is claimed.

## Bound authority, integrity and release

The model replacement is independently approved for base reconciliation. Root bound
the exact replacement and review authority before this UI release. No model behavior
was rerun solely for an unchanged authority copy; affected model / UI checks use the
exact replacement, actual source and named future prerequisites.

| Bound artifact | SHA-256 |
| --- | --- |
| C:/Users/eliza/Desktop/ClaudeFiles/hearsay/.superpowers/sdd/task-5b-receipt-partition-native-review-2026-09-13.md | `620be8b0603e11ac2279e0396c3d45005bb481d36faf61f069726811bcd0977a` |
| C:/Users/eliza/Desktop/ClaudeFiles/hearsay/.superpowers/sdd/task-5b-receipt-partition-review-validation-2026-09-13/file-inventory.json | `4d0d455a27b26184723eed54f4cc940e1c18a2bd120d2844fcc87f83104fc099` |
| C:/Users/eliza/Desktop/ClaudeFiles/hearsay/.superpowers/sdd/task-5b-receipt-partition-review-validation-2026-09-13/audit.json | `01f0ae6065a15e89395be85ed3525e3495cf60f29843c2d85c981e815eaa1df3` |
| Approved replacement source map | `d3b54a75415482ab5f5045940a43215093532ee42cd3943934b09b4a8b4e6b05` |
| Complete executed UI source map | `da05ead3493699c8b983aa79c809657af5132a8279344cb33c5e25b3816ad493` |

The final native audit verifies 592 original composition entries, 576 first-correction entries,
532 receipt-author entries, 365 fixed archive files, 29 exact transitive bodies,
18 exact draft bodies / disjoint chunk targets and all original case-name multiplicities.
The 8 explicitly copied provisional 638 artifacts retain their hashes. Source / copy
pairs and runtime identities remain exact. Both Git diff and cached diff are empty.

The final self-excluding file inventory covers this report, draft, constraints, all
owned maps, inputs, helpers, native logs, saved rendered markup and all 365 archived
files. Only its own file and owned disposable .vite* caches are excluded. Earlier
failed / provisional runs remain separate; reviewers use a fresh host and cache.

The UI proposal is released frozen for independent review. Actual predecessor
reconciliation, implementation gates, real browser evidence and accumulated code
review remain necessary. Root owns publication / index / commits; this author made
no production or shared-AI writes.
