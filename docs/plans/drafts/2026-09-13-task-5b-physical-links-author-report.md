# Task 5B physical extension author report

13 September 2026. Complete bounded proposal and native validation; independent proposal
review remains required. No production implementation, commit, push or gameplay gate is claimed.
The author's inherited capability matches the root; no child agent was launched.

## Delivered outcome

The draft contains exact complete bodies for evidence.ts, feature-links.ts and the new
physical-links.test.ts. Direct principal sightings, actual heard physical report atoms,
retained reference identity and recorded digest chronology are separate facts. The physical
branches preserve missing, late, unsupported and ambiguous outcomes, and do not invent
caster identity, séance causation, human speech, belief delivery, carriers or staffed work.
The old eight test files and seven unaffected model modules are byte-identical to the frozen
approved proposal. All 147 inherited identities and assertions remain; MIGRATED: none.

Final permanent arithmetic: 198 = 147 inherited + 51 physical/mixed additions.
The measured complete RED is 160 pass / 38 fail; complete GREEN is 198 pass / 0 fail.
Both compiler configs have zero diagnostics. All 33 virtual files have zero lint errors
and warnings. The actual changed source paths each fire the required four diagnostics:
Math.random and Date.now restrictions, new Date syntax, and forbidden content-rules import.

## Source basis and preserved inputs

Detached source base: aaaa57bd6bea2ce11414440f8c702173b53fb4b4. The new snapshot began under
owned validation/node_modules/hearsay-baseline, directly from that committed archive.
All 349 archived files remain hash-identical. The 16 named frozen primary inputs and their
owned byte-for-byte copies remain unchanged; the three governing brief hashes were checked
against their stated SHA256 values. Source and config never came from the moving primary.
No historical 96/144/147 wave was rerun; the new integrated 147 baseline did execute.

prepare.py, assemble.py and physical-amend.py make the assembly and every proposal edit
mechanically reproducible. The final source maps capture all executed bodies. Future 4C
capture/transport/evidence/ref/digest overlays are transcribed from the fixed tracked draft;
5A, 5A2, R16/R17 and ordinary model overlays come from the approved frozen map. Actual Task 3
physical/provenance unions remain intact. The historical provenance overlay duplicated an
already implemented field, so actual entry/provenance types supersede that narrow overlay.
T5A-A1 resolves the known threadOf seam by retaining inject/telling/seance membership while
excluding optional artifact claim IDs. These are explicit predecessor reconciliations,
not licensed production changes in this physical unit. Actual Task 4 auditor/T4-A1 execution
remains the predecessor owner's gate and is not claimed by this isolated model suite.

The host is virtual. No production src/tests/app/config files in either worktree were edited.
The isolated .probe.ts is the only collected test entry. Original TypeScript/ESLint configs
are used unchanged. Snapshot hashes are rechecked after every gate. Git tracked diff is empty.

## Native evidence

Commands run from the isolated worktree with pwsh and python -X utf8. Each run below has raw
binary stdout/stderr, native exit, exact command/cwd metadata and input-preservation results.
Final gates also save their exact source map beside their logs. Test execution uses the
existing ancestor Vitest installation; no packages or services were installed or contacted.

| Runner arguments after `python -X utf8 .superpowers/sdd/task-5b-physical-links-validation/run.py` | Result |
| --- | --- |
| `baseline tests baseline-native-access` | 147 pass, exit 0 |
| `red tests complete-red` | 160 pass / 38 fail, exit 1 |
| `green tests complete-green` | 198 pass, exit 0 |
| `green preflight complete-compiler-lint` | both real compilers 0; 33-file lint 0/0, exit 0 |
| `root lint final-firing` | four mandatory diagnostics per changed source path, exit 0 |
| `root audit final-input-audit-v2` | native exact-source/input integrity audit, exit 0 |
| `span-red tests span-red -t 'duplicate physical append rows cannot extend'` | one causal fail / 195 skipped, exit 1 |

The initial baseline Vitest invocation failed to load because esbuild could not read an
ancestor directory. The saved baseline-native logs preserve that failure; it was not RED.
The exact known read escalation then succeeded. No permission/configuration workaround
or root discovery change was used. Two early compiler diagnostics identified duplicate
historical provenance; a later author code diagnostic required a ChronicleEntry venue
guard. These are disclosed assembly/code fixes, not behavioral proof. The missing textual
digest anchor in the first assembly script was corrected to the actual next section.
The first captured integrity utility invocation used the snapshot working directory rather
than the author worktree and failed to locate its maps. final-input-audit preserves that
utility failure. Version 2 changes only the audit child cwd and metadata; its native audit
passes without rerunning or changing model tests, source or compiler configuration.

## Two adversarial author passes and local corrections

Pass 1 followed marker -> evidence -> actual receipt -> original sighting -> digest day,
then audited ordinary append reconstruction under the newly integrated physical union.
Real direct/delivered chains pass for both channels, while omitted atoms, missing receipt,
wrong original witness, duplicate evidence/sightings/receipts and unsupported cross-markers
remain honest. It exposed the mixed append defect: [network, night-visit, asking] caused
the old ordinary fold to misplace its child. The preserved first trace is
green/self-review-mixed-red (186 pass / 1 fail). Root adopted the bounded repair and will
queue R26. The strengthened final fixture retains the original guard hearing at tick 15;
complete RED falsely promotes the later received child to direct 15, while GREEN uses the
actual receipt at 45. Missing or ambiguous physical receipt slots reserve the candidate
inside the maximum possible span. Omitted physical speech and deduped physical evidence
keep ordinary receipt indexing intact. No recording or gameplay state is changed.

Pass 2 reversed the proof: missing histories, copied refs, simultaneous null-ID collisions,
maximum-span limits, old-suite equality, output ownership and exact executed bytes. It
caught an off-by-one scan: duplicate physical rows could let an ordinary child be associated
just beyond the maximum possible span. The saved span-red source map and selected failure
show received report 45 versus expected unknown. A strict bound now prevents that association;
the same permanent case passes in complete GREEN. Real simultaneous residue/night sightings
resolve to different evidence indexes despite matching legacy null-ID fields. The real
enemyDigest runaround consumer retains its physical marker; removing it cannot borrow the
ordinary twin. Real retellings preserve the original evidence object and first receipt date.

No existing assertion, seed, threshold or source physics was weakened. The initial physical
suite grew through disclosed author controls; intermediate raw logs retain their observed
counts but are not the final source certificate. complete-red and complete-green preserve
the exact final 198-body maps. The separately frozen span-red map captures its pre-bound fix.
case-registry.json proves all 147 inherited cases pass in both complete runs and all eight
old test file values remain identical. The nested ownership cases mutate returned marker,
feature/index/attention arrays and verify hashWorld equality and repeat-fold equality.

## Remaining limits and handback

This is a bounded evidence-arrival/reference proposal, not complete Task 5B. Physical
operation history and additional performed physical-operation/attention models, story/séance
model integration, calendar, terminal composition and UI remain separate authoring work.
A unique physical sighting yields no invented attention IDs; `none` means no proved
association rather than proof that unrecorded work was absent. Arbitrary corrupt-history
recovery is not promised outside the retained marker and maximum append-span boundary.
The principal identity used is the currently retained spymaster; no historical succession
metadata is invented. Proper typed retained state remains the input contract.

Separate independent review should focus on the mixed-slot bound, direct/remote uncertainty,
copied-marker boundary and source reconciliation. Actual predecessor reconciliation and
production gates/review remain required before implementation. No whole-game suite, build,
soak or Monte Carlo was rerun for this pure proposal, and no runtime count is inferred.

## Pasted native output

Complete GREEN:
```text

 RUN  v3.2.6 C:/Users/eliza/Desktop/ClaudeFiles/hearsay/node_modules/hearsay-physical-author-20260913/.superpowers/sdd/task-5b-physical-links-validation/node_modules/hearsay-baseline

 ✓ ../../green/entry.probe.ts (198 tests) 337ms

 Test Files  1 passed (1)
      Tests  198 passed (198)
   Start at  13:52:33
   Duration  1.64s (transform 342ms, setup 0ms, collect 934ms, tests 337ms, environment 0ms, prepare 79ms)

JSON report written to C:/Users/eliza/Desktop/ClaudeFiles/hearsay/node_modules/hearsay-physical-author-20260913/.superpowers/sdd/task-5b-physical-links-validation/green/complete-green.results.json
```

Complete compiler/lint:
```text
{
  "scope": "Task5B ordinary feature-link proposal fragment only; no implementation",
  "newSourceFiles": 9,
  "tsconfig.json": 0,
  "tsconfig.app.json": 0,
  "lintErrors": 0
}
```

Actual firing:
```text
Two actual scoped paths; four expected law diagnostics per path.
```

Full failed assertions and diffs remain in red/complete-red.stderr.log; bounded-span
failure remains in span-red/span-red.stderr.log. These are native model failures, not loader failures.

## Artifact identities

| Artifact | SHA256 |
| --- | --- |
| `task-5b-physical-links-author-draft.md` | `5c0f66b97e21aebff944e8e548d430c6ff0c8c500f4f0cf3c2d8b2fa72816018` |
| `task-5b-physical-links-constraints.md` | `afd0beea16821d82aac63f60820fb47a7d7edbf52ddb6756d5d9b2033c462778` |
| `task-5b-physical-links-validation/green/complete-green.source-map.json` | `e079229ac3f1088d91d95d72608ed50854bf09e4c7704a55869eeb939765b839` |
| `task-5b-physical-links-validation/red/complete-red.source-map.json` | `1e07857fcfbbb137a8c627ac67846bc4fcbb53a41ee46bd7ddd5ed0a111ebeed` |
| `task-5b-physical-links-validation/final-integrity.json` | `f77302142e787a9d418cd447282dd188517c1f169b0cf50837318ccee947bf02` |
| `task-5b-physical-links-validation/case-registry.json` | `222f9b09f37fe6f7f33116c8703bc5db85780ca3606b17f4e56ac8e353269798` |
