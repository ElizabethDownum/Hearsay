# Hearsay — recovery and plan audit, 5 September 2026

## Question and evidence

Ellie asked to read/audit the existing Hearsay plan and continue implementation,
preserving unfinished work from a stopped session. The actual repository was
ahead of the shared memory handoff: F1–F5/G1 were committed, with coherent G2
test edits still dirty. An independent premise audit reconciled this against
Git and the task ledger. Original bytes/hashes/diff were snapshotted locally.

The original Plan 9/10 were read and archived byte-for-byte under
`../plans/archive/`. A separate remaining-plan audit checked actual runtime
seams and the governing Plan 11 laws, then corrected its own overstatement about
missing receipt history after a focused controller challenge.

## State and decisions

G2–G4 were completed as `a458cd2`, `55a45ae`, `caa5a38`. The full controller gate
passed 1,713 tests/113 files, lint, types and build. Soak/MC reports matched the
previous baseline across every complete report block. Raw controller logs are
retained locally. The worker's report honestly notes that some earlier raw RED
stdout buffers expired; its mutation probes and restored file hashes are
documented, not substituted with invented output.

All R1–R11 decisions and the prior docket are preserved in
[the HTML review](../review.html). Load-bearing corrections:

- Task 2's spoken document marker must survive both Utterance and Observation
  types and every direct/delayed projection. Enemy forensics names the spoken
  hand, one hop at a time, including the avatar; anonymous/self source stays
  anonymous. The pure digest never reads artifact custody or chronicle directly.
- Scrying needs an explicit live venue sensor, distinct magic provenance, and
  persistent residue discovered locally and reported physically. Receipt cannot
  erase remote residue and reveal delivery status. One residue feature per venue;
  failed/omitted reports may retry after an actual later encounter.
- Scry captures the originally named speech/questions/presence channels. Scene
  presence is distinct from the existing watch-presence intel. Magic and a human
  observing the same telling count once for deductions; magic is not an extra
  informant channel. All code remains a draft at handoff.
- The departed needs separate generated/runtime historical metadata, not an id
  inserted into living witness lists or removed from the living graph. A truthful
  claim and one real subject-edge clue must survive session attachment.
- Further Task 4 audit found that enemy capture excludes presence entirely. A
  mechanically meaningful chapel-night sighting needs a bounded evidence/ref
  path. Controller selected direct local sighting or actual delivered speech,
  a pure night-visit feature deduped by kind/subject, ordinary exposure +1 and no
  carrier-profile auto-identification. Do not add a redundant nightVisits ledger.
  Detection must not inspect hidden séance state. Exact code is still draft work.
- Task 5 can derive receipt tick/final report copy from existing chronicle speech.
  Add only missing artifact-claim and per-hop report-root associations. Typed
  operation/report threads must explain more than ordinary rumor lineage.
- Counter-sketch overlay matches semantic attention/outcomes, not unrelated
  signal keys and opaque feature ids. Prose precedes UI; the existing panel
  import fence already covers debrief. The terminal gate needs behavioral proof.
- The forger arc must reach the actual two-council quorum. The old canary test
  proves a mechanism, not an ending; terminal divergence needs its own test.

Ellie later requested `$handoff` to restart the computer. Workers were ordered
to wind down; no new implementation was dispatched. Final readiness and restart
steps are in [the current handoff](../handoff.md).

## Lessons retained for later review

- Reconcile actual commits and byte-snapshotted unfinished work before trusting
  a stale session pointer. Recovery should preserve valid work, not replay tasks.
- Compare complete deterministic report blocks. Selecting expected lines can
  hide unexpected extra output; known-bad controls should include added/removed
  measurements as well as changed values.
- Challenge claims that history is absent before designing new recording.
  Final spoken reports already contained receipt chronology; identity through
  intermediate mutations was the narrower missing fact.

These candidate lessons remain project-local under Ellie's scope instruction.
No shared memory/skill changes, wiki indexing, publication or push occurred.
