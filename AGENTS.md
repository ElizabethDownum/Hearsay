# Hearsay project entrypoint

Follow the parent ClaudeFiles loader and canonical shared context instructions.
For the current project state, read `docs/handoff.md` before relying on the shared
AI project's older handoff. The 2026-09-05 session kept all writes inside Hearsay
at Ellie's explicit request, so shared memory was intentionally not updated.

The current working plan is `docs/plans/plan-9-current.md`; the review surface for
Ellie's deferred questions/issues is `docs/review.html`, built from
`docs/review/current.md` and the archived prior docket. Read these as project
state and decisions, not permission to expand the user's current task.

Keep new files and changes inside this checkout unless Ellie changes that scope.
Preserve existing work. One production writer and Git index owner at a time.
